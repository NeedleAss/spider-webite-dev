# Windows 现场进度（接手后维护）

初始状态：NOT RUN。2026-09-12 已开始 Windows W0 只读接收与盘点；Mac 证据仍只代表 Mac，本文件以下新增内容只记录本机实际执行结果。

## 当前交接点

- 当前阶段：W4 正交全向轮低速校准与 W5 网页手动控制已通过；W6 调参版 CAM 和主控均已烧录。最终主控为 `5233468c02fbcf2e-s5-follow`，静态背景 60 s 无手势动作误触发，动态人物/手势/健康与跟随复验仍待执行。
- 最近完成：降低健康、手势和人物候选门槛；跟随同时闭环距离与水平中心；加入 LIKE/DISLIKE/TWO/OK 动作。现场静态画面暴露高置信度背景伪手势后，增加近景手框与分级连续确认，保留原 FAIL 和中间版本证据；最终 Python 28/28、Node 14/14、全部 C++ 固件套件 PASS。
- 下一步：保持舵机 5 V 断开，先实测放宽后的 MAX30102 持续输出，再依次做近景手势与人物框静态测试；三项通过后才接通舵机电源，做低速 LIKE 跟随、DISLIKE 停止、TWO/OK 旋转及 ESTOP。
- 阻塞/限制：水平 RST 后 MPU 已恢复 `calibrated=true/tilt_fault=false/fault=false`；无 IMU 阻塞。舵机电源总限流仍按 2 A 对待，避免堵转和长时间四舵机同时动作；360°是 MPU 相对 yaw 估计而非编码器角度。
- 最近修改及原因：显示识别阈值保持 0.45；运动动作另要求至少 `80×120` 近景手框，LIKE/TWO/OK 连续 4 个动作确认帧，DISLIKE 连续 2 帧，同一手势离场 3 帧后才可重触发。CAM/person/IMU/command/output watchdog 截止期未放宽。
- 下一条人工操作：舵机 5 V 继续断开、不再按 RST；手指指腹完整覆盖 MAX30102 并轻压静置，开始无动力健康数据验收。无需切换电脑网络。

## 环境与设备

| 项目 | 实际值/证据 |
|---|---|
| Windows / Python / Git / Node | Windows build 26200.9445、25H2、PowerShell 7.6.5；原 Python 3.12 已被卸载导致项目 `.venv` 失效，主机测试/构建当前使用 Anaconda Python 3.13，守护烧录使用项目内 `build/toolchains/carerover-py310`（Python 3.10、esptool 5.1.0、construct 2.10.70、pyserial 3.5，`pip check` PASS）；Git 2.52.0；Node 24.15.0 / npm 11.12.1 |
| Arduino CLI / Core / 库 / FQBN | Arduino IDE 2.3.10；内置 CLI 1.5.1；`esp32:esp32` 3.3.10；SparkFun MAX3010x 1.1.2；主控实测 16 MB Flash/8 MB PSRAM，与 `esp32:esp32:esp32s3:FlashSize=16M,PartitionScheme=app3M_fat9M_16MB,PSRAM=opi` 一致 |
| ESP-IDF / ESP-DL 版本及 commit | ESP-IDF 5.3.4 commit `1b459d9...`、ESP-DL 3.3.11 commit `5d9c360...`、独立 Python 3.12 环境、CMake 3.30.5、Ninja 1.11.1.3 均已验证；CAM 四变体 compile_only PASS |
| 主控型号、Flash、PSRAM、串口 | COM6 CH340；ESP32-S3 QFN56 rev0.2，16 MB Flash、8 MB embedded PSRAM，MAC 尾部 `6824`；原 standalone X-drive 固件；备份后硬复位正常 |
| CAM 型号、Flash、PSRAM、串口 | COM3 CH340；ESP32-S3 QFN56 rev0.2，8 MB Flash、8 MB embedded PSRAM，MAC 尾部 `FEB8`；原 `carerover_gesture` 备份后继续约 124–125 ms 推理输出 |
| 主控 / CAM profile 路径（不写密码） | `config/board.local.json` 与 `config/cam-board.local.json` 均为 `windows_baseline_confirmed`；凭据仅保存在本机忽略文件中，不进入发布包 |
| Flash 与 NVS 备份路径、大小、SHA-256 | 初始 W1 清单仍在 `build/backups/windows-20260912-w1-backups.json`。2026-09-14 调参烧录前另保存 CAM 8,388,608 B SHA `5B0791E6...736DC`；最终主控写前 16,777,216 B 备份 `main-before-20260914-143203.bin` SHA `22E66A7B...A284E`。全部写入均在完整备份后进行并通过 esptool 哈希校验。 |
| 接线 / 电源 / 架空状态 | CAM/主控 USB、OLED GPIO4/5、MAX30102 GPIO8/9、四路舵机信号和 MPU6050 GPIO6/7 均已接；MPU6050 地址 0x68、VCC 来自主板 3.3 V。当前舵机外部 5 V 已断开、四轮架空；需把车身改为水平架空。俯视顺时针信号为 GPIO10→12→11→13，GPIO10/11 主动前后、GPIO12/13 主动左右。 |

## 阶段记录

| 阶段 | 状态 | 构建版本 | 原始证据路径 | 问题与下一步 |
|---|---|---|---|---|
| W0 接收/环境/构建 | PASS（ZIP 本体仍 NOT RUN） | 主控 source `fd240313f34d18fa` / Web `86015d4f6fc2c68f`；主控与 CAM 各四变体 compile_only | `output/windows/2026-09-12/W0/`；`build/stage5-*`；`build/cam-*-compile_only/manifest.json` | 253 文件哈希 PASS；Node 14/14、Python 25/25、全部固件主机测试 PASS；八个固件变体全 PASS。compile_only 包均禁止烧录 |
| W1 备份 | PASS | COM3 CAM 8 MB / COM6 主控 16 MB，均 8 MB PSRAM | `build/backups/windows-20260912-w1-backups.json`；`output/windows/2026-09-12/W1/` | 两板完整 Flash 与主控 NVS 已读取、定长和 SHA-256；NVS 与完整镜像切片一致；尚未写设备 |
| W2 CAM | PASS_WITH_OBSERVATION | gesture PASS；vision 人物实测 PASS_WITH_OBSERVATION；stream 无线/MJPEG PASS | `build/cam-stream-windows_baseline_confirmed/`；`output/windows/2026-09-12/W2/`、`W3/cam-correct-ssid-20s.jsonl` | 最长人物丢失约 4.078 s 已记录，follow 必须按丢目标停车；用户截图确认浏览器实时视频与手势叠加显示 |
| W3 observe | PASS | `4c1af62cbe71895b-s5-observe` / Web `86015d4f6fc2c68f` | `build/stage5-observe-4c1af62cbe71895b-device/`；`output/windows/2026-09-12/W3/` | 用户确认 OLED、MAX30102、网页/WS/MJPEG，截图可见实时视频、手势、SpO2/SQI、IMU 与机器人状态；截图瞬间 HR 为空，不伪造具体心率值 |
| W4 校准 | PASS（低速初始标定） | `build/calibration/binaries/motion_calibration.ino.bin` SHA `93BBCD47...2C776` | `build/calibration/compile.log`；`config/calibration/windows-2026-09-13.json`；`output/windows/2026-09-13/W4/post-confirm-reboot-4s.jsonl` | 三自由度地面测试 PASS，NVS 已确认并重启读回；长距离分轮 span 精调移入 W5 |
| W5 manual | PASS_WITH_DIAGNOSTIC（供电改线后启动安全门禁通过） | `b296a68c1979e553-s5-manual` | `output/windows/2026-09-13/W5/manual-imu-main33v-servo-on-15s.jsonl` | MPU VCC 改接主板 3.3V 后，舵机 5V 接通仍 `calibrated=true/tilt_fault=false/fault=false`；CAM 链路正常，速度为零；保留原始六轴诊断字段 |
| W6 follow | STATIC_GUARDS_PASS；动态动作 NOT RUN | CAM stream 已烧录；主控 `5233468c02fbcf2e-s5-follow` 已烧录 | `build/stage5-follow-5233468c02fbcf2e-device/`；`output/windows/2026-09-14/W6/` | 60 s 静态背景 697 行、gesture_action=0、协议错误=0、CAM valid=601/bad=0；当前非水平架空造成 `roll≈-75.7°/FAULT`，需水平 RST 后再做人物、健康和动力动作实测，不能据静态证据宣称自动跟随通过 |
| W7 十分钟无线 | NOT RUN | — | — | — |

## 修改与测试追加记录

每项记录：时间、复现步骤、预期/实际、原始 FAIL 日志、改动文件与原因、相关回归、设备结果、遗留项。修复后保留旧失败记录，不覆盖成 PASS。

### 2026-09-12 W0 接收与只读盘点

- 解压清单：使用现有 Python 3.13.5 运行 `python tools/verify_handoff.py`，结果 `PASS: 253 source/reference/evidence files match the exported SHA-256 manifests.`。这只证明解压文件完整，不是实物验收。
- ZIP：同目录只有 `CareRover_Windows_Handoff_2026-09-12.zip.sha256`，内容期望值为 `906f6a55661eff26dc606efc1acc60bcb44b484f344eb7483cf80f76de62fc4`；ZIP 本体不在现场，故 ZIP SHA 状态为 NOT RUN。
- Windows 主机测试：`npm test` PASS 14/14；`python -m unittest discover -s tests -p "test_*.py" -v` PASS 20/20。后者使用 Anaconda Python 3.13.5，不替代锁定的 Python 3.12 环境复现。
- Windows 固件主机测试：`python tools/check_firmware.py` 中 `Safety controller: all assertions passed, including 20 fault scenarios`，随后 GCC 6.3 在 `firmware/main_wireless/imu_filter.h:37` 以 `-Werror=misleading-indentation` 编译失败，tracking/其后套件未运行。预期为全部套件通过；实际为 FAIL。原源码暂未修改，下一步应先采用现代 C++17 编译器复现，若仍失败再做最小格式兼容修复并保留本记录。
- 工具链：Arduino IDE 2.3.10、内置 CLI 1.5.1、ESP32 Core 3.3.10 已安装；`arduino-cli` 未在 PATH，但可使用 IDE 内置可执行文件。项目 `.venv` 不存在，`tools/windows.cmd environment` 按设计拒绝运行。锁定 ESP-IDF 5.3.4 未准备；现存 5.4.4 不能替代。
- 设备：`SerialPort.GetPortNames()` 只有 COM4/COM5；PnP 显示两者为蓝牙串口。历史 `USB-SERIAL CH340 (COM3)` 与 `(COM6)` 均为 Disconnected，没有当前连接的 ESP32-S3。未执行任何串口写入、Flash 读取、烧录或运动命令。

### 2026-09-12 W0 环境补齐、设备映射与 Windows 构建修复

- Python：从 python.org 获取官方 Python 3.12.10 x64 安装器，SHA-256 `67B5635E80EA51072B87941312D00EC8927C4DB9BA18938F7AD2D27B328B95FB`，Authenticode 为 Python Software Foundation 且 Valid。项目 `.venv` 为 Python 3.12.10；pyserial 3.5、aiohttp 3.14.3、Pillow 12.3.0、construct 2.10.70、esptool 5.1.0，`pip check` PASS；`tools/windows.cmd environment` PASS 并生成 `build/environment-report.json`。
- 主机测试修复：GCC 6.3 对 `imu_filter.h:37` 同行的两个独立 `if` 报 `-Werror=misleading-indentation`。仅将其拆行，未改条件与行为；随后 tracking、safety 20 个故障场景、PWM 校准/原子拒绝/截止期/初始化失败全部 PASS。旧 FAIL 保留于 `output/windows/2026-09-12/W0/firmware-check-fail.txt`。
- 工具回归：Arduino CLI 在 Windows 把本机编码字节直接写入 `compile.log`，原工具强制 UTF-8 读取会遮蔽真正错误。先加入 GB18030 日志回归测试，再由 `read_log_text()` 按 UTF-8/系统编码/GB18030 解码并最终替换无效字节；Python 全套由 20 增至 21 项且 21/21 PASS。
- Unicode 路径：长路径构建实际在 linker 处以 `cannot open output file ... No such file or directory` 失败；原始日志为 `output/windows/2026-09-12/W0/observe-unicode-path-fail.log`，SHA-256 `CBD19B6D10A4103E642EA699B5668600B75FB9247481CD74BB64C18FF747C7CC`。使用 `subst R:` 映射当前唯一源码目录，并让构建工具保留调用路径而非 `resolve()` 回中文路径；未复制/分叉源码。
- observe compile_only：从 `R:` 运行 Stage 5 observe 完整 PASS；Flash 1,067,666/3,145,728（33%），动态内存 55,384/327,680（16%）。构建 manifest 为 `build/stage5-observe-fd240313f34d18fa-check/manifest.json`；留档 `observe-short-path-pass.log` SHA-256 `C084E1075746B26FC39B0B3957B8FC47155AE1999F8F9FA5B087F13C02F2201E`、manifest SHA-256 `CDA7E678FB4262AE0C3019100456315A952AD8E4B700F70479342F32FAD10925`。该包标记 compile_only，禁止烧录。
- manual compile_only：PASS；Flash 1,074,990/3,145,728（34%），动态内存 55,408/327,680（16%）。`manual-short-path-pass.log` SHA-256 `45039E8A24396093B3A2799159D41373B9C49B4A1964150A8245ED334A044F64`、manifest SHA-256 `DF94446F2596B16DFFFC497C14D905C423CBEF7C22FF0D242B6839CA4F360891`。
- follow compile_only：PASS；Flash 1,075,338/3,145,728（34%），动态内存 55,408/327,680（16%）。`follow-short-path-pass.log` SHA-256 `9EE0457BCD689164FB923E89C0ECF3EE6167EF4924101F057E3ADF2A7015B0A6`、manifest SHA-256 `4DCBDF4C9EF5E67E09AF1195A15D76C06818058A01AD12959F8967DADED7A7BB`。
- legacy compile_only：PASS；Flash 1,041,245/3,145,728（33%），动态内存 55,208/327,680（16%）。`legacy-short-path-pass.log` SHA-256 `E9AE5108AC90B57DBE43C041EBC73633AEDED31C9CDE43E7BA54738226BB1B95`、manifest SHA-256 `328A56D7F9639FDE08CFDC00A3605546D57BECDCA2A6754429C654117817D5EB`。
- 设备重新枚举：当前 CH340 COM3/COM6 均 Started。只读、无串口写入采样表明 COM3 正运行 `carerover_gesture`，COM6 正运行 standalone X-drive，故完成角色映射；仍未用 esptool 复位读取 Flash/PSRAM，避免在舵机 5 V 开启时触发主控复位。
- CAM 工具链：ESP-DL commit `5d9c360...` 与 ESP-IDF 5.3.4 commit `1b459d9...` 已正确取得；25 个递归子模块已补齐并通过洁净性验收（项目级 `core.longpaths=true` 仅用于 Windows 超长文件名）。ESP-IDF `required` 交叉工具包安装退出 0；官方 Ninja 1.12.1/CMake 3.30.2 已落在项目工具树，但交接锁要求 CMake 3.30.5/Ninja 1.11.1.3，尚未覆盖验证。专用 IDF Python 环境因审批未创建，CAM 四变体仍 NOT RUN。

## 现场执行计划（W0–W7，不重复软件设计）

1. **W0 环境出口**：在短英文工作路径或确认当前 Unicode 路径可用的前提下，安装 Python 3.12，创建 `.venv`，安装锁定 requirements；调用 IDE 内置 Arduino CLI 1.5.1，确认 Core 3.3.10、下载 SparkFun 1.1.2；准备 ESP-IDF 5.3.4/ESP-DL 3.3.11 及锁定 CMake/Ninja；重跑清单、Node/Python/C++ 和 observe/manual/follow/legacy、CAM 四变体 compile_only 构建。只把 Windows 实际结果写为 PASS。
2. **设备唯一识别**：舵机外部电源断开，一次只接一块板；枚举 VID/PID/COM，读取芯片/Flash/PSRAM 信息，将主控 16 MB 与 CAM 8 MB 映射后才建立 `.local.json`，不保存密码到公开证据。
3. **W1 备份**：用项目工具对已唯一识别的每块板做完整 Flash 备份，单独保存主控 NVS，记录大小和 SHA-256并验证可读；备份失败绝不烧录。
4. **W2 CAM**：依次 gesture→vision→stream（pico 只用于对比），烧录后采集至少两分钟 UART；人工展示手势/人脸并打开 MJPEG，记录 G/P CRC、FPS、推理耗时、最低内存和掉帧。
5. **W3 observe**：只读聚合固件先验收 AP/WS、视频地址、人物框、MPU6050 五秒静置校准、MAX30102 和 OLED；此阶段不初始化 PWM、不转轮。
6. **W4 架空校准**：确认稳定舵机电源、全系统共地、四轮架空和车头标记；逐轮实测 neutral/invert/span，保存、`confirm_calibration`、断电复测，模板 null 只用实测值替换。
7. **W5 manual**：在安全区域完成前后、左右横移、顺逆旋转各两秒，并测松手/关页/断网/CAM失联/倾斜/ESTOP；保留命令时刻、PWM停止时刻和实际轮子停止证据。
8. **W6 follow**：仅在 W5 通过后测试静止目标、前后、横移、缓行和离开；基于真实日志增量调参，丢目标/低置信度/故障必须停车且不自动恢复。
9. **W7 无 USB 十分钟**：使用满足电流能力的外部供电，同时运行视频、手势、健康、OLED、IMU、运动至少十分钟，记录最低 heap/PSRAM、FPS、故障和无线演示；未提供真实电压采样时低压保护继续标记 NOT RUN。

### 2026-09-12 continuation: executable Windows next-step handoff

- Added [`docs/WINDOWS_NEXT_COMMANDS.md`](WINDOWS_NEXT_COMMANDS.md), containing the electrical safety gate, MPU6050 GPIO6/7 wiring, user-terminal ESP-IDF environment commands, CAM four-variant compile-only commands, and W1→W7 field order.
- A project-local `.venv` pip install was attempted and rejected by the host approval limit; no workaround or alternate package source was used. No board reset, Flash read, backup, erase, flash, or motion command was issued.
- Current actionable gate remains: user runs the official ESP-IDF environment install in a local terminal, confirms servo 5 V is off/wheels raised, and confirms MPU6050 wiring. After that, resume CAM compile-only, then W1 identity/backup.

### 2026-09-12 W0 completion and W1 backup

- User confirmed MPU6050 wired to 3.3 V/GND/GPIO6/GPIO7, servo 5 V disconnected, common ground retained, and all wheels raised.
- Toolchain separation: the general project `.venv` retains esptool 5.1.0 for guarded board backup/flash. ESP-IDF uses its own `build/toolchains/idf-tools/python_env/idf5.3_py3.12_env` with the official v5.3 constraints. The active CAM build uses ESP-IDF v5.3.4, CMake 3.30.5, and Ninja 1.11.1.3.
- Windows CAM feedback-loop failures were retained instead of overwritten: `cam-gesture-path-fail.log` SHA `25F4B5C3...`, `cam-gesture-unicode-fail.log` SHA `00B2D3AF...`, `cam-gesture-ccache-unicode-fail.log` SHA `100A098D...`, `cam-gesture-subst-physical-path-fail.log` SHA `295A6D92...`, `cam-gesture-managed-component-unicode-fail.log` SHA `FF3BEF88...`, and `cam-gesture-stage-missing-main-wireless-fail.log` SHA `734F5043...` under `output/windows/2026-09-12/W0/`.
- Root causes/fix: CMD `%PATH%` expansion discarded the exported compiler paths; Python Kconfig opened UTF-8 JSON through GBK; ccache and linker helpers could not consume resolved Chinese paths; the first staged source omitted the shared `firmware/main_wireless` include. `tools/tracking.py` now uses UTF-8 child processes, disables Windows ccache, safely renders console text, and stages the CAM source, shared protocol code, managed components, and only required ESP-DL directories under ASCII `%TEMP%` before copying verified release files back. Python regression coverage increased to 25/25.
- CAM compile-only PASS packages and app sizes: gesture 3,427,136 B (manifest SHA `920DE448...`), vision 3,629,536 B (`3D343169...`), stream 4,193,984 B (`199628A6...`), pico 4,473,376 B (`B7006CAC...`). Every package contains the expected addresses 0x0/0x8000/0x10000 and all file hashes were recomputed successfully. Evidence copies are `output/windows/2026-09-12/W0/cam-*-compile-only-{pass.log,manifest.json}`.
- Final regression: Python 25/25 PASS; safety controller including 20 fault scenarios PASS; tracking protocol/follow/safety/IMU/JPEG leases PASS; PWM calibration/rejection/deadline/init-failure PASS; Node 14/14 PASS.
- Device identity (esptool 5.1.0): COM3 = ESP32-S3 QFN56 rev0.2, 8 MB Flash, 8 MB embedded PSRAM; COM6 = ESP32-S3 QFN56 rev0.2, 16 MB Flash, 8 MB embedded PSRAM. Both hard-reset normally with servo power disconnected.
- W1 backups PASS: CAM full 8,388,608 B SHA `80d2bac61336873ffb62383b193beed451347dc53966868b401b60bb6c7bb8ad`; main full 16,777,216 B SHA `c21a38b1d3efdc45b7c27dcfa84981a6331ff5abdc1d0afc80167893e4c1c96c`; main NVS 20,480 B SHA `1f55ffcddc1fce4d4ab43d09da1f8e58730a19bf3aadd78331c3eaaa8b9b4410`. The separate NVS exactly matches bytes 0x9000–0xDFFF of the full main image. Metadata is `build/backups/windows-20260912-w1-backups.json`; backup binaries remain local/private.
- Parsed current partitions: CAM has NVS 0x9000/24 KiB, PHY 0xF000/4 KiB, factory app 0x10000/7 MiB, FAT 0x710000/960 KiB. Main exactly matches the target NVS/OTA/app0/app1/FFat/coredump layout, confirming the NVS extraction offset.
- Post-backup passive serial: COM3 continued `carerover_gesture` inference at approximately 124–125 ms with stable heap/PSRAM; COM6 standalone motion firmware produced no unsolicited periodic lines during the 12 s window. No serial command and no device write was issued.

### 2026-09-12 W2 CAM gesture device build and guarded flash

- The user supplied an eight-character private AP password. It was written only to `config/cam-board.local.json` and `firmware/main_wireless/wifi_secrets.h`; the password value is intentionally omitted here. The release exporter already excludes both files, and the unpacked project now also has explicit local ignore rules for `config/*.local.json` and `wifi_secrets.h`.
- `cam_profile()` accepted the local CAM profile as `windows_baseline_confirmed`. The device gesture build completed with ESP-IDF 5.3.4; app size is 3,427,888 B within the 7 MiB app partition. Manifest SHA-256 is `C3EE4CEEA0948302443924BDC41F6567F2500BCDCB4F0E9B9A9201913C58210E`.
- The guarded flash path re-identified COM3 as the same ESP32-S3 CAM (MAC suffix `FEB8`, 8 MB Flash, 8 MB embedded PSRAM) and read a new 8,388,608 B full backup before any erase. `build/backups/cam-20260912-163838.bin` SHA-256 is `80D2BAC61336873FFB62383B193BEED451347DC53966868B401B60BB6C7BB8AD`, identical to the W1 CAM backup; metadata SHA-256 is `E63564589747B4BA90E4D4285E8A2515BE316463ED04E66B3470268F74F7A989`.
- Bootloader at 0x0, partition table at 0x8000, and gesture app at 0x10000 were written and each passed esptool's post-write hash verification. COM3 was re-identified after hard reset. Main controller COM6 was not accessed and servo 5 V remained disconnected.
- A 20 s passive COM3 baseline captured 145 lines: 133 CRC-valid gesture frames, 0 bad CRC, no hand present, gesture throughput mostly 7.11 FPS, reported inference 132 ms, zero capture drops, minimum heap 150,051 B, and minimum PSRAM 3,955,356 B. Wi-Fi was false as expected because the main-controller AP is not running yet. Evidence is `output/windows/2026-09-12/W2/cam-gesture-device-baseline-20s.jsonl` (SHA `C93E72E0CABDCBA6C5D03E1091C5C3573D70D6923F85852CF502DA0407338A45`) plus its summary (SHA `EF3B18BC5B852123ACB5E6176F42C3C5BE99198BB3C24FD3B6376FA288857B59`).
- Gesture functional acceptance is still NOT RUN until a hand is held in frame for the planned two-minute capture. Do not advance to vision merely from the no-hand baseline.

### 2026-09-12 W2 gesture acceptance and vision transition

- Gesture functional acceptance PASS. The 120 s OK-hand capture contains 485 CRC-valid G frames, 0 bad CRC, and 0 sequence-gap events. OK appears in 455 frames (93.81% overall; 97.01% of hand-detected frames), with mean confidence 0.982, minimum confidence 0.589, and longest uninterrupted OK run 21,609 ms. Mean inference time is 243.2 ms, maximum 247 ms; metrics report zero capture drops. Raw log SHA is `2D0690CFA9B0474428EEE409CCB9F3B2480FAD249128BBAF05449C46742597D6`; derived analysis SHA is `80DBB500202ED970194B003711709146ABDC24289686EF5194FD340E0C564CF9`.
- Before replacing gesture, the guarded flash path saved `build/backups/cam-20260912-170356.bin`, an 8,388,608 B rollback image with SHA `CB7AAEB1DEA55C8D64593D4924499AADCA275F502D56C5F6100930D79FD53BDC`.
- Vision device build PASS: app size 3,630,272 B, manifest SHA `1A620423453F87884A515671D09E2B6CD13B05E9445A9CB6A05A1D501FBEEA28`. The expected three partitions were written to COM3 and all post-write hashes passed; COM3 identity remained the verified 8 MB CAM.
- Vision empty-scene 20 s baseline captured 211 CRC-valid G/P frames and 0 bad CRC. All 105 P frames correctly reported missing; face throughput was approximately 5.24–5.28 FPS, face inference 38–40 ms, gesture inference 132 ms, zero capture/JPEG drops, minimum heap 150,051 B, and minimum PSRAM 3,827,612 B. Raw log SHA is `4A346CE6BF8799D38D6DC278EA5BE480B7809CECB17A78FD966CD9AFC2A42A44`.
- Vision functional acceptance remains NOT RUN until a person is held in frame for two minutes. Servo 5 V remains disconnected and COM6 has not been changed.

### 2026-09-12 W2 stream baseline and W3 observe build diagnostic

- Stream device build PASS: app size 4,194,720 B, manifest SHA `BFC384E2B56E889C018DF58E6B60CC663F37B1778DCAF80D1175530A76A86FA1`. Before replacement, the guarded path saved `build/backups/cam-20260912-172804.bin` (8,388,608 B, SHA `8C8878A04720093F027EA0C7E9165DC1BC057032A4B965E6094FD4767CD5678E`).
- Stream no-AP 20 s baseline captured 168 CRC-valid G/P frames, 0 bad CRC, 84 person-missing P frames, and 49–103 JPEGs in successive metrics windows. Typical gesture/face throughput was 3.7–4.3 FPS, JPEG about 2.8–3.0 FPS, zero JPEG/capture drops, minimum heap 37,899 B and minimum PSRAM 2,516,928 B; `wifi=false` is expected before the main AP exists. Raw log SHA is `FAE3692CD54E973BF33FDF237A08FF2F8708ED5F81D1DB4788D8F999ED4C4D56`.
- First W3 `tools/windows.cmd build --profile config\\board.local.json --stage 5 --integration observe` attempt failed in `ld.exe` because Arduino's `--build-path` resolved to the Unicode physical project path. The failure log also contained a character not representable in GBK, causing the old diagnostic print to raise a second encoding error and obscure the root cause. No COM6 operation or device write occurred.
- Minimal fix applied in `tools/carerover.py`: `windows_main_build_directory()` stages the generated sketch/objects/webroot/binaries under ASCII `%TEMP%\\CareRover-main-build`, then copies the verified package back; error/success diagnostics now pass through `console_safe()`. Added two staging and one console regression tests in `tests/test_tooling.py`. The fix is not yet re-run because the current execution environment rejected the required elevated test/build call after its usage limit was reached.
- Safe rerun command (after permission recovery): `tools\\windows.cmd build --profile config\\board.local.json --stage 5 --integration observe`. Require `Build package` and `NO DEVICE WAS FLASHED.` before COM6 flash. Servo 5 V remains disconnected.

### 2026-09-12 W3 Observe device integration and Windows field diagnostics

- The Unicode build fix was re-run against the original device command and passed. Package `build/stage5-observe-fd240313f34d18fa-device/` compiled at 1,067,666 B Flash / 55,384 B globals and passed an independent manifest/partition verification before its first flash. A later MPU address-compatibility build produced `build/stage5-observe-4c1af62cbe71895b-device/`, 1,067,746 B Flash / 55,384 B globals; manifest SHA-256 is `F15ECEEC578BC727FB20AE48D7BE691B8AC1D2D2C0E1883D31B843FFB6D7CE39`.
- COM6 was re-identified as the expected 16 MB / 8 MB PSRAM main board before both writes. The first complete pre-flash backup is `main-before-20260912-212518.bin`, 16,777,216 B, SHA-256 `C21A38B1D3EFDC45B7C27DCFA84981A6331FF5ABDC1D0AFC80167893E4C1C96C`. The second is `main-before-20260912-215351.bin`, same size, SHA-256 `B86DA7F73148187243DCA29156EABF6A28D5B9D56237D08E8944B53D428927B4`. All written partitions passed esptool post-write hashes; the second write left FFat untouched.
- A first passive capture saw the prior runtime state with AP down. The new `tools/tracking.py capture --reset` option deliberately toggles only EN through RTS while leaving GPIO0 out of download mode. Its full boot capture proved Arduino Core 3.3.10, 16 MB Flash, 8 MB PSRAM, MAX30102 ready, OLED ready, and `ap_ready` at `192.168.4.1`. The tool passed Python syntax checks and the existing tooling regression suite.
- AP naming was corrected from the previously assumed textual MAC suffix. The running firmware computes the low 16 eFuse bits and broadcasts `CareRover-EE68`; CAM local configuration still named `CareRover-6824`, explaining `wifi=false`. The local CAM profile was corrected without documenting the password. The rebuilt stream package is ESP-IDF 5.3.4, app 4,194,720 B with 43% of its 7 MiB app partition free; manifest SHA-256 is `71701311092966956DB10323312705817612A8EB65BD68DBF51716DBAA0E8A46`.
- COM3 was re-identified as the expected 8 MB CAM, then backed up to `cam-20260912-221135.bin`, 8,388,608 B, SHA-256 `AF186502ED858767AE7251F984DEDC11C7653451D81689495DA8CF30C3134AB7`. Bootloader/partition/app writes completed and the app hash verified. The 20 s post-flash capture contains 160 CRC-valid G/P frames, 0 bad CRC, continuous `wifi=true`, gesture/face about 3.4–4.3 FPS, JPEG about 2.4–3.0 FPS, zero JPEG/capture drops, min heap 36,979 B and min PSRAM 2,516,928 B. Raw SHA-256 is `8B0EDA95FF43BCB8F6DF497AD8BF9D1F7FC730416C0DE361375E4EFE3D957995`.
- MPU6050 initially remained invalid after power-only reconnection. The driver now safely probes both 0x68 and 0x69 and reports the selected address, but both probes still failed until the user rewired VCC/GND/SDA/SCL. After rewiring, the task recovered without reboot at address 0x68 and reported `valid=true`, `calibrated=true`. An initial 29° roll correctly latched the 25° tilt fault; after the module was levelled, integrated capture held pitch about -3.9°, roll about 5.1°, `tilt_fault=false`.
- Final 20 s main capture ended at `mode=IDLE`, `fault=false`, `ap=true`, zero velocity, CAM `vision_link valid=5819/bad=0/stale=0/resync=1`, and MPU valid/calibrated. MAX30102 was sampling but the incidental finger placement was `poor_signal`; stable HR/SpO2 display remains a deliberate human test. Raw SHA-256 is `04B2E9BEEFC40C8941A6D43657D52D1E87A2EAEF588CC5A65D8C51464E583D16`.
- Windows Wi-Fi scan found `CareRover-EE68` at 99% on 2.4 GHz channel 1. A first connection request was immediately superseded by the saved campus-network policy, so no 192.168.4.x HTTP result is claimed. At the user's request, no further network switching was attempted. The CareRover profile is manual; the temporary clear-text profile XML was removed. Browser/WS/MJPEG visual acceptance remains NOT RUN.
- Final non-network regression: Python 28/28, Node 14/14, firmware safety (20 fault cases), tracking/follow/IMU/JPEG leases, and PWM calibration/rejection/deadline/init-failure all PASS. Calibration firmware compiled from the single maintained drive sources; `motion_calibration.ino.bin` is 320,736 B, SHA-256 `6EABA5D5BE2317A48B829B8F28BCBD897955AEA1BD801B5D3D790BF3C7CBF8DB`. It was not flashed and no PWM was initialized.

### 2026-09-12 W3 browser acceptance and W4 pre-calibration gate

- User completed the four requested field checks and supplied a browser screenshot at `http://192.168.4.1/?transport=ws&video=mjpeg`. It visibly confirms the CareRover page, live 320×240 CAM image, gesture overlay, health panel, IMU/robot status and MJPEG transport. The user also confirmed OLED and MAX30102 operation. W3 is therefore PASS; the screenshot moment shows SpO2/SQI but no numeric heart-rate value, so no HR value is inferred.
- Four servo 5 V leads are now energized and the user confirms the wheels remain raised. The active main build is still Observe, which deliberately does not initialize PWM. A passive 12 s COM6 capture issued no reset and no serial command; it retained `vx=vy=wz=0`, `last_cmd_ms=0`, AP ready and a clean vision link ending at valid=39923/bad=0/stale=0. Evidence is `output/windows/2026-09-12/W4/pre-calibration-servo-powered-observe-12s.jsonl`, SHA-256 `DF1F2E341D14C0C4794AE9E54CE1A68064A83765824459B49A5A5F22A4F44D4B`.
- The same capture found two expected safety blockers rather than motion: after the browser station disconnected, `stop_reason=wifi_station_disconnected`; MPU6050 remained electrically valid/calibrated at address 0x68 but was physically tilted, ending near pitch −3.54° / roll −129.27° with `tilt_fault=true`. It must be fixed level and the main board reset while stationary before W5/manual.
- Actual top-view clockwise servo-signal order is user-reported as GPIO10→12→11→13. Existing code still maps logical FL/FR/RL/RR to 10/11/12/13, so the physical start corner and marked front must be confirmed before changing the one authoritative pin array. No calibration firmware was flashed and no PWM/motion command was sent.
- The servo supply continuous/peak current rating is not yet known. The earlier discussed 2 A total-limit module is not accepted for four-servo simultaneous drive; W4 movement remains NOT RUN until the exact supply rating is confirmed.

### 2026-09-13 W4 level recovery and physical wheel map

- After the user levelled the chassis, pressed main-controller RST and held it stationary, a passive 12 s COM6 capture confirmed MPU6050 address 0x68, `valid=true`, `calibrated=true`, `tilt_fault=false`; it ended near pitch −9.54° / roll −6.21°. Main remained Observe/IDLE with `fault=false`, `vx=vy=wz=0`, `last_cmd_ms=0`; CAM link ended at valid=1256/bad=0/stale=0/resync=0. Evidence: `output/windows/2026-09-13/W4/post-rst-level-check-12s.jsonl`, SHA-256 `899CFC1A47549302D5A3B638F93096579CC179FB704C6D8B1D860B2FD1533857`.
- Physical map is now unambiguous: with the marked front at the top, FR is GPIO12 and the reported clockwise order is 10→12→11→13, hence FL/FR/RL/RR = GPIO10/12/13/11. Added the single authoritative `firmware/main_wireless/motion_layout.h`; both standalone calibration and manual/follow drive initialization consume this same map.
- Added an NVS layout ID. Calibration saves the current layout ID, and main motion accepts `verified=true` only when the stored layout matches. Calibration values from the prior 10/11/12/13 assumption are ignored instead of being silently applied to different physical wheels.
- Regression PASS after the change: Python 28/28, Node 14/14, safety/tracking/follow/IMU/JPEG host suites and PWM calibration/rejection/deadline/init-failure suites. Updated standalone calibration compiled successfully: app 321,280 B, SHA-256 `473508687479BA817E93B6770BC642CB303A2DD587D9EFBF6844E9673F964A7B`.
- Parallel manual/follow device builds were cancelled without device access after long-running Windows compile workers; they are not counted as PASS. They will be rebuilt sequentially after physical per-wheel calibration, so the final packages contain the measured values/layout verification state.
- User chose staged servo testing. For the first test, only the FL GPIO10 signal is to remain connected; GPIO12/GPIO13/GPIO11 signals must be unplugged at the controller while all wheels remain raised. No calibration image has yet been flashed and no wheel command has been issued.

### 2026-09-13 W4 calibration flash and DISARMED gate

- User confirmed only the FL GPIO10 signal remains connected, servo 5 V is energized and all four wheels are raised. COM6 was re-identified as the expected ESP32-S3 rev0.2 with 16 MB Flash / 8 MB PSRAM before writing.
- A new full 16 MB read at default baud was cancelled at 4.9% because the projected duration exceeded 20 minutes; a 921600-baud retry failed at 41.7% with an esptool corrupt-data error. Neither attempt produced a completed backup file and neither is claimed as recovery evidence. Existing verified full backups from 2026-09-12 remain available.
- A current pre-calibration NVS backup PASS: `build/backups/main-nvs-before-calibration-20260913-191527.bin`, 20,480 B, SHA-256 `23E03EF04477AB9B280493F7A5D5853AB9F68F54738F47129CFC04D6702DC7CD`.
- Arduino CLI 1.5.1 / ESP32 Core 3.3.10 uploaded the verified calibration package to COM6 using the exact device FQBN. Bootloader 0x0, partitions 0x8000, boot_app0 0xE000 and app 0x10000 each passed post-write hash verification; NVS and FFat were not written. App SHA-256 is `473508687479BA817E93B6770BC642CB303A2DD587D9EFBF6844E9673F964A7B`.
- A post-flash `status` query reports `ready=true`, `armed=false`, `estop=false`, `moving=false` and logical FL/FR/RL/RR pins 10/12/13/11 with default 1500 µs neutral. No `arm` or wheel motion command has been sent. Physical GPIO10 neutral observation is still required before the first bounded low-speed pulse.
- FL/GPIO10 physical calibration: 1500 µs is stationary; +10% (about 1530 µs), 400 ms remained inside the observed dead zone; +20% (about 1560 µs), 500 ms rotated in the wheel direction that drives the chassis forward; −20% (about 1440 µs), 500 ms rotated in the direction that drives it backward. Auto-stop and explicit disarm both returned `moving=false`; the user requested and the tool issued a second +20%/−20% bounded pair, also ending `armed=false`. The repeated physical direction/stop observation is pending user confirmation before the FL result is finalized.
- FL/GPIO10 repeated physical verification PASS: the second +20%/−20% pair again produced forward/backward wheel directions with a complete stop between and after pulses. Provisional raw electrical sign is non-inverted; final chassis-level sign remains subject to combined X-drive validation.
- FR/GPIO12 physical calibration: 1500 µs is stationary; +20% for 500 ms and 1500 ms produced the user-observed rightward rolling component; −20% for 1500 ms produced the opposite leftward rolling component. Auto-stop and explicit disarm passed after every pulse. Because one radial X-drive wheel produces a diagonal force component, this raw observation is retained without claiming final chassis invert until combined motion is tested with adequate power.
- RL/GPIO13 physical calibration: 1500 µs is stationary; a combined bounded sequence produced +20% for 1500 ms as the user-observed leftward component, stopped, then −20% for 1500 ms as the rightward component. Firmware reported `moving=false` after each deadline and ended `armed=false` after explicit disarm. Final chassis invert remains pending combined X-drive validation.
- RR/GPIO11 physical calibration: 1500 µs is stationary; +20% for 1500 ms produced the user-observed backward component, stopped, then −20% for 1500 ms produced the forward component. Firmware reported `moving=false` after each deadline and ended `armed=false`.
- The four raw directions are symmetric for the radial X-drive: FL +→front, FR +→right, RL +→left, RR +→back. The provisional logical-forward signs derived from the supplied CAD geometry are `[+1,-1,+1,-1]`. While DISARMED, NVS was written with all neutrals 1500 µs, all wheel spans 300 µs, FL/RL non-inverted and FR/RR inverted. `save` deliberately left `verified=false`; main manual/follow motion therefore remains inhibited until pair/full chassis validation and explicit `confirm_calibration`.
- Front-pair GPIO10+GPIO12 raised-wheel validation PASS. With the provisional signs applied, `f 20 1000` produced the expected FL forward / FR opposite-left component; after an automatic stop, `b 20 1000` reversed both. The user confirmed both directions and stops matched expectation. Firmware ended `armed=false` and `moving=false`.
- User then reconnected all four servo signals and reported that all four servos can move normally. A defensive `disarm` plus `status` query confirms `ready=true`, `armed=false`, `moving=false`, neutral 1500 µs on every wheel and the provisional invert pattern `[false,true,false,true]`. Four-wheel simultaneous commands remain gated on the still-missing servo-supply current rating; if it is the previously discussed 5 V / 2 A module, no four-wheel concurrent test is authorized.
- At the user's explicit request, limited 2 A supply trials were allowed only with the chassis raised: four-wheel `f 20` pulses of 300 ms (twice) and 600 ms (once), each with deadline stop and explicit disarm. The 600 ms observation was GPIO10/GPIO11 showing the forward component and GPIO12/GPIO13 showing the leftward-looking component. For the radial X-drive these are the two diagonal rolling components whose lateral parts cancel during forward translation; no direction change was made.
- Four-wheel `r 20 600`, stop, then `l 20 600` was run twice, each ending `armed=false`, `moving=false`. All wheels moved; the user described the visible directions as right-forward then left-back. Interpretation is pending a required clarification: if the chassis remained raised, these visible diagonal wheel components are expected and do not prove chassis drift; if the chassis was on the floor and actually translated diagonally, per-wheel span matching is required. No rotation test or `confirm_calibration` has been issued.

### 2026-09-13 W4 orthogonal-omni root-cause correction

- The chassis is not a radial X-drive. The user clarified the physical constraint: GPIO10/GPIO11 wheels are actively driven longitudinally and passively roll laterally; GPIO12/GPIO13 wheels are actively driven laterally and passively roll longitudinally. Therefore the prior four-wheel X-drive matrix necessarily injected a longitudinal component into a lateral command, reproducing the observed right-forward / left-back path.
- A regression was added before the fix. On the old matrix, `tools/check_firmware.py` failed twice at the same assertion: a pure longitudinal request incorrectly commanded the GPIO12/front-right lateral wheel. This deterministic result confirms a kinematic-model mismatch rather than random supply sag or floor slip.
- The authoritative mixer is now `mixOrthogonalOmni`: logical FL/FR/RL/RR correspond to GPIO10/12/13/11 and use `w10=vx-wz`, `w12=vy-wz`, `w13=vy+wz`, `w11=vx+wz`, followed by peak normalization. Thus pure `vx` energizes only GPIO10/GPIO11, pure `vy` only GPIO12/GPIO13, diagonal motion superposes the two independent axes, and `wz` energizes both pairs with opposing moments.
- Corrected active-axis polarity is FL10 non-inverted, FR12 non-inverted, RL13 inverted, RR11 inverted. `MOTION_LAYOUT_ID` was changed to `0x4F4D4E32` (`OMN2`), so the prior X-drive calibration cannot be silently reused.
- Updated code and handoff material: `firmware/main_wireless/omni_kinematics.h`, `continuous_servo_drive.cpp`, `motion_layout.h`, `firmware/motion_calibration/motion_calibration.ino`, its README, `tests/firmware/drive_test.cpp`, `docs/tracking-development.md`, `docs/WINDOWS_AI_HANDOFF.md`, `config/calibration/template.json`, and new field record `config/calibration/windows-2026-09-13.json`.
- Post-fix regression PASS: Python 28/28, Node 14/14, safety/tracking/follow/IMU/JPEG and orthogonal-omni drive suites. The corrected calibration app is 321,280 B, SHA-256 `B0E1DB6C7A815876C587AC219A350306B301F4423AFAEE2A89510DDB9F7D0AEC`; bootloader, partitions, boot_app0 and app all passed post-write hash verification on COM6.
- Device startup reports `layout=orthogonal_omni_2x2`. While disarmed, NVS was set to neutral 1500 us on all wheels, invert `[false,false,true,true]`, span 300 us, and saved with `verified=false`. Status ended `ready=true`, `armed=false`, `estop=false`, `moving=false`. The earlier X-drive pair/four-wheel trials remain historical electrical evidence only and do not count as final chassis calibration.
- Remaining W4 acceptance: reproduce the original floor scenario with the corrected firmware; pure forward/back must not actively drive GPIO12/GPIO13, pure lateral must not actively drive GPIO10/GPIO11, then validate bounded rotation and match loaded wheel spans. Do not run `confirm_calibration` before those checks pass.
- Corrected floor longitudinal validation PASS at 20%: `f 20 600`, automatic stop, then `b 20 600`, automatic stop. Every post-deadline status returned `moving=false`; the sequence ended with explicit `stop`, `disarm`, `armed=false`, `estop=false`. The user observed straight forward followed by straight backward, with no reported diagonal drift.
- Corrected floor lateral validation PASS at 20%: `r 20 600`, automatic stop, then `l 20 600`, automatic stop. Only the GPIO12/GPIO13 axis was commanded by the new mixer. Every post-deadline status returned `moving=false`; the sequence ended with explicit `stop`, `disarm`, `armed=false`, `estop=false`. The user observed straight right followed by straight left, with no reported longitudinal drift.
- A supply-bounded floor rotation test was then issued because rotation uses all four servos: `cw 20 300`, automatic stop, then `ccw 20 300`, automatic stop. Every post-deadline status returned `moving=false`; the sequence ended with explicit `stop`, `disarm`, `armed=false`, `estop=false`. Physical clockwise/counter-clockwise response awaits user confirmation.
- The user observed the first sequence as counter-clockwise then clockwise when viewed from above, proving that only the `wz` contribution was reversed relative to the published `wz>0 = clockwise` contract. A new field regression changed the expected positive-wz wheel vector to `[+,+,-,-]`; before the production fix, `tools/check_firmware.py` deterministically failed at that clockwise assertion while its safety and tracking suites still passed.
- The mixer rotation terms were corrected to `[vx+wz, vy+wz, vy-wz, vx-wz]`; longitudinal and lateral terms were unchanged. Documentation and the Windows handoff contract were updated together. Post-fix C++ firmware suites PASS, Python 28/28 PASS, and Node 14/14 PASS.
- Corrected calibration app: 321,280 B, SHA-256 `93BBCD47A8FB8957FE778C6D2B7EBE52D9F3735332D268929CB967EE7CF2C776`. COM6 was explicitly stopped/disarmed before upload; bootloader and partitions were unchanged/verified, changed app sectors were written and the app hash verified. NVS persisted neutral 1500 us, invert `[false,false,true,true]`, span 300 us, still `verified=false`; post-boot status was `armed=false`, `moving=false`.
- The corrected floor rotation validation PASS: the reissued `cw 20 300`, stop, then `ccw 20 300`, stop was observed from above as clockwise followed by counter-clockwise, predominantly in-place with no obvious translation. All deadlines and the final explicit stop/disarm passed.
- W4 low-speed kinematic acceptance now has three clean degrees of freedom on the floor: straight forward/back, straight right/left, and clockwise/counter-clockwise in-place rotation. No visible axis drift was reported at 20%, so the common 300 us span is accepted as the initial low-speed value; longer-run loaded span tuning remains a W5 refinement rather than a blocker for guarded manual integration.
- While stopped and disarmed, `confirm_calibration` returned both save and confirmation ACKs. A subsequent safe EN reset booted `orthogonal_omni_2x2` with `ready=true`, all measured parameters intact, `armed=false`, `moving=false`, and no layout-mismatch/reverification warning. Evidence: `output/windows/2026-09-13/W4/post-confirm-reboot-4s.jsonl`, SHA-256 `71EE6693CDDCB27C400775669922799F69B3E9E6C1502FDB9AF4A79270BF4852`. W4 is PASS for guarded low-speed integration.

### 2026-09-13 W5 Manual build, flash, and IMU startup gate

- Sequential Stage 5 Manual build PASS: `06322de705aa2c6a-s5-manual`, Web `86015d4f6fc2c68f`; sketch uses 1,075,098 B (34%) and globals 55,408 B (16%). Package `build/stage5-manual-06322de705aa2c6a-device` independently passed checksum and partition verification; app SHA-256 is `D8815381D0E18C8CD1DFAFF9090750525578099721941F55A51FDFB59F2F2966`.
- Before replacing calibration firmware, the confirmed NVS was read to `build/backups/main-nvs-after-calibration-20260913.bin`, 20,480 B, SHA-256 `98F71D8F6AD7A98F8B4AD712022DC27FF08DA8DEE1B8310364B61463F5B33FC2`. Existing verified full rollback images remain available. Manual bootloader/partitions/app were uploaded with post-write hash verification; NVS was not written. FFat was not rewritten because its installed and package Web version is unchanged (`86015d4f6fc2c68f`).
- A 12 s reset capture confirmed main boot, MAX30102 ready, AP `CareRover-EE68`, CAM frames with zero bad CRC, and all velocity targets zero. MPU6050 remained electrically valid at 0x68 but did not finish calibration, so the safety controller correctly held `mode=FAULT`, `fault=true`, `tilt_fault=true` and prevented motion. Evidence SHA-256: `1B78BE2CE789DE1640FBD7ED8EB0FFAA5BBACD1F739810016C645EAD3F0EB057`.
- An additional 30 s passive capture still ended `valid=true`, `calibrated=false`, with level-ish pitch about 0.4° and roll about -7.7°, and zero requested velocity. This rules out a merely short startup delay. Evidence `manual-imu-wait-30s.jsonl`, SHA-256 `CEE87025C3B783D4ECC25B7CB2A6D0772D156149F3E1A3AFFE05A4C65D8E493D`. Next diagnostic is to disconnect only servo 5 V, keep MPU wiring/common ground, level the chassis, press main RST and hold still for at least 8 s; this tests servo vibration/power coupling without weakening the IMU or tilt thresholds.
- After the user reported RST pressed and the still interval complete, a 12 s passive capture still showed MPU address 0x68 and `valid=true` but `calibrated=false`; motion remained inhibited in FAULT with zero targets. Pitch/roll stayed near -10°/-3° for most of the capture but changed abruptly near the end to approximately -9°/+11°, indicating physical movement or vibration sufficient to invalidate a five-second calibration window. Evidence `manual-imu-servo-off-12s.jsonl`, SHA-256 `06E9C14E2FBB6C12E83F01FA8F75A5AED5F9DD7A334B379A38BC1200C1E32D3C`. The user's reply did not explicitly confirm servo 5 V was disconnected, so that physical condition must be confirmed before attributing the failure to firmware or changing thresholds.
- The user then explicitly confirmed servo 5 V disconnected. A controlled EN reset with the chassis untouched completed MPU calibration at about 7.2 s: address 0x68, `valid=true`, `calibrated=true`, `tilt_fault=false`, bias `[-1.0117,-1.4574,0.4228] dps`. The safety state automatically recovered to `IDLE/fault=false`; velocity remained zero and CAM packets stayed valid. Evidence `manual-imu-confirmed-servo-off-reset-12s.jsonl`, SHA-256 `F26476CB2EFCA2B0E30F1FCBF8D7833CA151AB5C7DBB8ACA1B636FE5B1B2D262`.
- The user then explicitly confirmed servo 5 V disconnected. A controlled EN reset with the chassis untouched completed MPU calibration at about 7.2 s: address 0x68, `valid=true`, `calibrated=true`, `tilt_fault=false`, bias `[-1.0117,-1.4574,0.4228] dps`. The safety state automatically recovered to `IDLE/fault=false`; velocity remained zero and CAM packets stayed valid. Evidence `manual-imu-confirmed-servo-off-reset-12s.jsonl`, SHA-256 `F26476CB2EFCA2B0E30F1FCBF8D7833CA151AB5C7DBB8ACA1B636FE5B1B2D262`.
- Root cause boundary: the IMU thresholds and wiring are capable of passing unchanged; the failed startup was caused by physical disturbance and/or servo-power vibration during the required continuous five-second still window. Operational startup order is therefore: servo 5 V off, level and reset main, wait for `calibrated=true/fault=false`, then energize servos without another reset. No IMU or tilt threshold was weakened.
- To distinguish electrical noise from mechanical vibration, a diagnostic Manual build was completed with raw `accel_g` and `gyro_dps` fields in `imu_status`: package `build/stage5-manual-b296a68c1979e553-device`, app 1,075,456 B, SHA-256 `5D281AE3BB7880AFDE0CD67ACBE499F1D7D89385F7A15651E9724B90F1D194D4`. Host regressions remained green. Upload was not performed because the host automatic approval quota rejected the serial write; the previously flashed Manual firmware remains active and no workaround was attempted.
- After permission recovery, the diagnostic Manual app was uploaded to COM6 with hash verification; NVS and FFat were not written. A 12 s servo-powered capture exposed one impossible raw sample amid otherwise stable readings: normal `accel_g≈[0.10,-0.04,0.93]`, `gyro_dps≈[-1.0,-1.5,0.45]`, but one sample `accel_g≈[0.10,-0.03,-0.02]` with filtered `pitch≈-70°/roll≈-116°`. The safety fault is therefore a genuine sensor-data corruption event, not a real chassis tilt. Evidence `manual-imu-raw-servo-on-12s.jsonl`, SHA-256 `58FF48DA708D53C509B4F9D5F72CB9F2EAF94D3707C543BF707D38F3235846AB`.
- Electrical corrective action: power MPU6050 VCC from the main board's regulated 3.3 V, keep its GND tied to main-board GND and the servo-supply GND, and reserve the shared module's 5 V output for servos. Do not weaken tilt thresholds. Re-test after rewiring; if the bus remains clean, retain the diagnostic fields as observability and proceed to W5 web control. If corruption persists, add plausibility rejection for impossible accel/gyro frames before any movement is enabled.

### 2026-09-13 W6 Follow device boot and web-test boundary

- Stage 5 Follow was built and uploaded without changing NVS/FFat: package `build/stage5-follow-b296a68c1979e553-device`, app 1,075,808 B, SHA-256 `C05B46115AF114027C008C75AC939444481C93D4F3F6F309CC6CEE5E6FFD627A`; manifest integration is `follow` and includes `PERSON_FOLLOW`. A 15 s boot capture with servo 5 V on reached `calibrated=true`, `tilt_fault=false`, `mode=IDLE`, `fault=false`; CAM frames remained valid and all targets stayed zero. Evidence `follow-boot-15s.jsonl`, SHA-256 `6AD224C1129713FEAFBF5D7AE760CB6D5323383F88E3AA37F5DBC16EC2652703`.
- The computer was briefly connected to `CareRover-EE68` for a read-only reachability check, then returned to the user's original `Tsinghua-Secure` network when the user requested no network switching. No webpage motion or follow command was sent; browser acceptance remains pending user-operated testing.

### 2026-09-14 W6 field tuning after user-operated web test

- 用户在另一设备完成网页现场操作，确认数据与控制路径可用，同时复现五类问题：底盘轻微波动触发 IMU 停车；MAX30102 有波形但 HR/SpO₂ 难以同时稳定显示；手势与人物检测偏保守；原跟随器把大偏差转向与距离控制互斥；缺少 LIKE/DISLIKE/TWO/OK 动作。
- 先建立失败回归而非直接改阈值。`tests/firmware/tuning_test.cpp` 在旧逻辑上稳定报出六项 FAIL：单次 35°加速度冲击锁止、0.50 人脸拒绝、偏心时距离与中心修正不能同时输出、现场记录窗口 HR=83/SpO₂=73 被质量门拒绝。旧失败控制台保留在本轮会话记录；生产修复后 `python tools/check_firmware.py` 四套 C++ 测试全部 PASS。
- IMU：故障门槛改为原始或融合 pitch/roll 达 40°并持续 200 ms；恢复门槛 30°且稳定 1 s。单帧底盘加速度冲击被抑制，持续 45°仍锁止。IMU 无效、未校准或超过 100 ms 未更新的原保护不变。
- MAX30102：候选门槛调整为采样 20–30 Hz、AC/DC 0.00002–0.030、频谱 SNR≥2.0；SpO₂ 另需相关性≥0.30、R=0.20–1.80、候选 65–100。首次输出仍需两个相邻稳定窗口；HR 相邻差≤12 BPM、SpO₂≤6%，获得有效值后可保持最多 8 个短暂坏窗口。手指移开/采样或报告过期仍清空。此策略用于比赛演示和趋势观察，不构成医疗有效性声明。
- 手势：主控进入/保持阈值改为 0.45/0.25，进入和切换均需连续 2 帧，分类缺失宽限 7 帧、无手宽限 3 帧；网页“已稳定”门槛同步为 0.45。CAM 手检测候选阈值由 ESP-DL 默认 0.25 调到 0.20。
- 人物与跟随：CAM 人脸模型候选阈值 0.42，UART/主控跟随阈值统一 0.45，IoU 跟踪下限 0.03、歧义差 0.02。控制器同时计算 `vx`（框面积相对进入时 A0）和 `wz`（框中心水平误差），不再在偏心时暂停距离闭环，也不再用 `vy` 造成横移/转向竞争。CAM/人物 490 ms 来源截止期保持不变。
- 手势动作：稳定标签边沿 `LIKE` 启动自主人物跟随（可等待下一张合格人物帧最多 2 s），`DISLIKE` 停止回 IDLE，`TWO` 顺时针、`OK` 逆时针；360°由 MPU relative yaw 累计并处理 ±180°环绕，300°后降速，360°完成或 12 s 超时停止。同一持续标签只触发一次；ESTOP、IMU/CAM/网络/输出租约仍优先。
- 回归结果：C++ safety/tracking/drive/tuning 全部 PASS，Python 28/28 PASS，Node 14/14 PASS。项目原 `.venv` 因已卸载 Python 3.12 失效；主构建临时使用 Anaconda Python 3.13 并补齐锁定 `construct==2.10.70`。CAM 交叉编译器仍存在，另用本机 Python 3.10.18 在项目工具树建立 `idf5.3_py3.10_env`，未删除旧环境。
- 最终主控设备包 `build/stage5-follow-1fe7fa5dc6ae2a8b-device` 校验 PASS：固件 `1fe7fa5dc6ae2a8b-s5-follow`，Web `e828373be196e42b`，app 1,077,330 B，app SHA-256 `F130A7CA0E38516F789E86CD2DF52F542E70815566F6DC160B843A4174571A02`，FFat SHA-256 `C26E09FFC350DDE2BB50EC51F743F5AB15AFC54508CDEECBA561C13AC5FFDE44`；16 MB/OPI/stage 5/follow manifest 与分区校验 PASS。未烧录。
- 最终 CAM stream 包 `build/cam-stream-windows_baseline_confirmed` 校验构建 PASS：ESP-IDF 5.3.4、ESP-DL 3.3.11，app 4,194,784 B（7 MiB app 分区余 43%），app SHA-256 `0D36C18DC3A9C93CBC1B1800FF609E31C1DE43DD4A3CE1FC0F8706132E47C61C`；8 MB 布局。未烧录。
- COM 只读盘点仍为 COM3/COM6 两个 CH340（历史唯一身份：COM3 CAM 8 MB，COM6 主控 16 MB）。未切换 Wi-Fi。下一现场门槛：舵机 5 V 断开、四轮架空后分别 guarded flash；主控复位后静置约 8 s完成 MPU 校准，再接通舵机 5 V，先测试健康/手势/人脸静态输出，再按低速顺序测试 LIKE 跟随、DISLIKE 停止、TWO/OK 旋转和 ESTOP。
- 为避免现场依赖失效，另在 `build/toolchains/carerover-py310` 创建隔离烧录环境，锁定 construct 2.10.70、esptool 5.1.0、pyserial 3.5，并通过 `pip check`；旧 `.venv` 保留未删除。CAM 编译临时 `R:` 映射已在构建完成后移除。

### 2026-09-14 W6 tuned device flash, false-action diagnosis, and level restart

- 用户确认舵机 5 V 断开且四轮架空后，守护流程在任何擦写前重新识别设备并做完整备份。COM3 仍为 8 MB/8 MB PSRAM CAM、MAC 尾 `FEB8`；`cam-20260914-130642.bin` 为 8,388,608 B，SHA-256 `5B0791E66512999D4D7274E154885E0EDB1DD416E604C99AA4ABA47C95B736DC`。CAM stream app 0x10000 写入及 bootloader/partition 均通过 esptool 哈希校验；芯片复位后再次识别一致。
- 初版调参主控包和 FFat 已写入 COM6，全部分区通过写后哈希；写前完整镜像 `main-before-20260914-132228.bin` 为 16,777,216 B，SHA-256 `6A181C23F60F4567F1C1DAE4DB2467DE71E8E097EAAA6A54C86DFA0F7FBB204E`。NVS 未擦写，已确认的正交全向轮 neutral/invert/span 参数保留。
- 首次 15 s 启动证据 `tuned-main-boot-15s.jsonl`（SHA `122DECF62E7B7A42D7B1E04B1B72E9E65A97B7DA99EF167E658B04D9397A5E78`）证明主固件、AP、OLED、MAX30102 和 CAM 数据链均工作，但架空方式使 MPU 约 `pitch=-10.5°/roll=-68.0°`，安全控制器正确保持 FAULT、速度为零。该结果不是“轻微波动误停”，故未继续放宽 40°/200 ms 倾角门槛。
- 新 CAM 20 s 被动记录 `tuned-cam-baseline-20s.jsonl`（SHA `16AA28103CD13FB55FD3E8FF76E20E1F9CA81F688ED2C3E01EBB6AD01ABFA5CA`）共 133 行，约 2.8–3.25 FPS，JPEG/capture drops 均为 0。初版主控却在静态背景误收一次 `DISLIKE`；真实旧 OK 证据 455 帧的框最小 `101×165`、平均 `148×217`，本次第一组 8 个小框伪 DISLIKE 最大仅 `40×71`。据此在动作层增加 `80×120` 近景框门槛，显示层 0.45 阈值不变。
- 近景框门槛后，30 s 记录仍发现背景偶尔形成约 `123×146` 大伪框，并连续两帧给出 DISLIKE。保留中间证据 `final-action-guard-static-30s.jsonl`（SHA `EA27F4ECE6E9AD9D376B8438A57DEC271CC0CA8E434EAA829C7249777854C5B5`），没有把它改写为 PASS。最终动作确认改为：LIKE/TWO/OK 连续 4 个动作帧、DISLIKE 连续 2 帧、离场 3 帧才重置；停止保持更快，任何启动运动的动作更保守。
- 最终主控包 `build/stage5-follow-5233468c02fbcf2e-device` 独立 checksum/partition verify PASS：固件 `5233468c02fbcf2e-s5-follow`，app 1,077,610 B，app SHA-256 `157C46B7B9C5BE51C5FFCA27AA132826F95A7D7A606A2161231112520E76A1E7`，manifest SHA-256 `4FDFB6BEFBAB09C89CC105EC20AFED5AE8CF087E5E88D21F1C74E582BD050148`。最终写前镜像 `main-before-20260914-143203.bin` 为 16,777,216 B，SHA `22E66A7BF42B1F6E12A421A9322AFB56C206601B1A6F88B9C2B37163EC2A284E`；只写固件分区，NVS/FFat 未触碰，所有写后哈希通过。
- 最终静态背景 60 s 证据 `final-motion-action-guard-static-60s.jsonl`（SHA `12B2A350B663A2E96ABB3EFD450A183905B2267035D4F23DE50C2C5FB7E580C5`）共 697 行：gesture_action=0、movement action=0、protocol error=0，vision link 结尾 valid=601/bad=0/stale=0/resync=0，所有速度为零。该证据只通过静态防误触，不替代真实 LIKE/TWO/OK 功能测试。
- 用户随后把车身水平架空、舵机 5 V 仍断开，按主控 RST 并静置。`final-level-post-rst-15s.jsonl`（SHA `A206EBF88DFB95B8809F019410A1274E7AAEAFCA6A3D3B1B0AF3A0D1CB3513FD`）确认 `valid=true/calibrated=true/tilt_fault=false`、`mode=IDLE/fault=false`、速度为零、手势动作与协议错误均为 0，CAM 结尾 valid=838/bad=0/stale=0/resync=0。采样中短时陀螺约 30°/s、加速度约 1.47 g 的扰动没有触发倾角故障，符合本轮“单次/短时扰动不过早停车”的目标。
- 本轮最终回归：C++ safety/tracking/drive/tuning 全部 PASS，Node 14/14，Python unittest 28/28。项目 `.venv` 因系统 Python 3.12 已卸载而失效，已明确记录；主机测试/构建使用 Anaconda Python，串口守护工具使用独立 `carerover-py310`，未伪称旧 `.venv` 可复现。

### 2026-09-14 W6 MAX30102 原始波形诊断、心率修复与暂停点

- 成功的人体健康采样 `output/windows/2026-09-14/W6/final-health-finger-90s.jsonl` 共 90 组报告：HR/SpO₂ 两项均连续有效，SQI 平均 99.78、最低 80；但候选 HR 在约 40 与 80–89 BPM 之间跳变，确认“有数值”已解决而“半频误判”仍未解决。
- 第一次原始波形命令因串口脚本先打开端口再关闭 DTR，产生一次非预期 EN/DTR 重启；`RAW_ON` 在启动前丢失，日志 `final-health-raw-finger-45s.jsonl`（SHA `8BD887A36BC28E4E1F4A7E999455ED89F061B68F7DC54BC9CB1E8296588E18F0`）只有启动/校准信息，不作为波形证据。该事实已向用户说明，未据此修改阈值。
- 修正串口打开顺序（先设 DTR/RTS，再 open）后自检 `boot_lines=0`，`STATUS` 确认固件 `5233468c02fbcf2e-s5-follow`、`IDLE/fault=false`、OLED/MAX30102/CAM 在线。
- 成功原始采样 `final-health-raw-finger-30s.jsonl`：749 个 `ppg_raw`、31 组 health、2 个命令回执（RAW_ON/RAW_OFF）、0 个启动行，SHA `45CD529CEA2BEF51B3B5A11D479F339E039B8E0D5F352FC4080940432A74B135`。离线重放显示 0–8 s 缓慢压力/基线变化时频谱最低 bin 会误报 40 BPM；稳定窗口的时间域自相关连续给出约 79–88 BPM。
- 增量修复（尚未烧录）：新增 `firmware/main_wireless/ppg_rate_estimator.h`，以 45–150 BPM 自相关局部峰作为 HR 主判据，排除慢漂移边界峰；新增 `Median5Filter`，对 HR/SpO₂ 候选做五窗口中值；HR 坏窗宽限由 8 s 增至 12 s，SpO₂ 保持 8 s；health 串口新增 `pulse_corr` 诊断字段。手指移开仍走原有立即 reset 路径，未改安全运动门禁。
- 回归：`tools/check_firmware.py`（safety/tracking/drive/tuning）PASS；Python unittest 28/28；Node 14/14。新设备包 `build/stage5-follow-17dbe0423e82ee1a-device` 通过 checksum/分区校验，固件 SHA `814E5500EA75C3AF50C8A4E1BBB941350182775C93E5A9A07F41FE83C1E6F5AF`，manifest Web `079a3a0e1913dd71`；当前开发板仍未写入该包。
- 用户要求暂停时，守护烧录已在 COM6 完整 16 MB 写前备份读取阶段约 48.6%（约 8.1 MB）处中断；未执行 `write-flash`，未改变设备。既有可回滚完整镜像 `build/backups/main-before-20260914-143203.bin`（16,777,216 B，SHA `22E66A7BF42B1F6E12A421A9322AFB56C206601B1A6F88B9C2B37163EC2A284E`）仍有效。恢复后下一步：舵机 5 V 保持断开、四轮架空，使用同一包重新执行 guarded flash（可复用该完整备份或按工具重新读取），再做约 20 s 启动/IMU 安全采样与 60–90 s 手指复测。

### 2026-09-14 W6 MAX30102 心率连续性第二轮现场修复

- 恢复后守护烧录重新从头完成 COM6 的 16 MB 写前备份；`main-before-20260914-172605.bin` 为 16,777,216 B，SHA-256 `D71E679A8E1B2A663F68AF0C71BF888AA804436EED11B6D271E1B401E8E37C22`。随后仅写入 `build/stage5-follow-17dbe0423e82ee1a-device` 的固件相关分区，NVS 和 FFat 保留，所有 esptool 写后哈希通过。
- `post-ppg-fix-boot-20s.jsonl`（SHA `294ACDEDE727371EC8F792A3F56FAD081730D226C0AFBF63994E524F4E3366DA`）确认固件 `17dbe0423e82ee1a-s5-follow`、MPU6050 `valid/calibrated=true`、`tilt_fault=false`、`IDLE/fault=false`、速度全零且 CAM 链路正常。
- 第一轮 60 s 人体证据 `post-ppg-fix-finger-60s.jsonl`（SHA `A92A9C116579A7E0A7F1BAE2D73C4E50A186F12A483E818292D19157CCFE5E6F`）含 60 组 health，手指与 SpO₂ 60/60 有效；HR 33/60 非空、72–81 BPM、均值 76.88 BPM，候选 40 BPM 为 0，证明半频误判已修复。首次 HR 在约 17.3 s 获得，但此后出现 10 个报告窗的连续空值；12 窗保持只覆盖其中前段，连续显示仍未完全达到现场要求。全程运动输出为零，IMU 无坏帧/倾倒故障。
- 第二个增量修复把 HR 已可信后的坏窗保持由 12 提高到 30 个报告窗；`StableMetric` 新增可选输出斜率限制，HR 每窗最多变化 3 BPM、SpO₂ 每窗最多变化 1%，手指移开仍立即清空。测试新增对现场 21 s 低相关漂移、重新捕获收敛和斜率限制的回归；C++ safety/tracking/drive/tuning、Python 28/28、Node 14/14 全部 PASS。
- 第二版设备包 `build/stage5-follow-3883d0b1154903e9-device` 校验 PASS：固件 `3883d0b1154903e9-s5-follow`，Web `079a3a0e1913dd71`，app SHA-256 `BD075C8DB9F9E7B41B88F04239D3ED1EEC76DA80FEDFCC199BAD793A8986392C`，manifest SHA-256 `6A1D09F1D433055B4D4B5426E509334678ADE73F301E52B0CCAA7CC3E117D5D9`。
- 第二版写入前完整备份 `main-before-20260914-204143.bin` 为 16,777,216 B，SHA-256 `A8F56FA0FD5C3FEC328D09FE47CBA061D9DC98FFFB74948C6E676EBA51D7FC07`。守护流程再次识别 COM6 为既有 ESP32-S3 QFN56 rev0.2、16 MB Flash/8 MB PSRAM、MAC `68:ee:8f:60:68:24`，仅写固件分区且全部哈希通过，NVS/FFat 未触碰。
- 第二版 20 s 启动证据 `post-health-hold-fix-boot-20s.jsonl`（SHA `8528C8D08F7D1EBB6C54A0D6F973880B402AAD74CB3CC0799227E0FDA7A5EE0F`）确认目标固件运行，MPU 有效且已校准、倾倒故障为 false，系统保持 IDLE、故障 false、速度全零；CAM 链路坏帧/过期/重同步均为 0。
- 随后的 `post-health-hold-fix-finger-60s.jsonl`（SHA `B416608C2725C5EF2419C4A63537AC3F3DD567869D65544B1E442D5A6AF4282F`）60/60 报告均为 `no_finger`，`finger_present=0`、IR 约 400；这不是低可信脉搏窗，不能用于评价第二版 HR/SpO₂ 连续性。该记录仍证明 60 s 内运动输出为零、IMU 无坏状态、CAM 结尾 `valid=825/bad=0/stale=0/resync=0`。下一步必须先确认手指确实覆盖 MAX30102 的 LED/光电窗口，使 `finger_present=true`，再重新采集 60–90 s；不得用降低手指存在阈值掩盖无光学接触。
- 重新放置后先用 `post-health-hold-fix-contact-check-8s.jsonl`（SHA `9383C06E1EAB6A96E8CA7FC47886782ED2F4E33C05156C7E0A57719120B2B647`）确认真实光学接触：8/8 报告 `finger_present=true`，IR 约 123,342–124,342，SpO₂ 已显示 92、SQI 100；随后才进入长采样。
- 有效人体复测 `post-health-hold-fix-finger-valid-60s.jsonl`（SHA `EC304230989D7C025C097E7B44BFC89AEA45C928F7D85C435B7879A99A813830`）含 60 组 health，手指 60/60、整体 valid 60/60、SpO₂ 60/60 非空；SpO₂ 为 85–100%，相邻报告最大变化 1%，符合新斜率限制。HR 在采集开始时延续一段先前 49 BPM 的保持值，30 窗宽限耗尽后有 9 个空窗；约 18 s 以连续两个合格窗口重新建立 53 BPM，此后余下约 42 s 心率不再空缺。重新建立后即使连续坏窗计数达到 28 仍保持 66 BPM，之后候选重新捕获并以每个连续报告最多 3 BPM 收敛至 72 BPM；候选范围 49–80 BPM，40 BPM 候选为 0。跨越空值/重新初始化的 49→53 不属于连续输出斜率限制。
- 同一 60 s 有效复测中，所有 `wireless_status` 的 `vx/vy/wz` 均为 0、`fault=false`；60 组 IMU 均有效且无倾倒故障；CAM 结尾 `valid=2437/bad=0/stale=0/resync=0`。因此第二版达到“已获得可信心率后短中期坏窗持续显示、重新捕获平滑、血氧持续输出”的比赛演示目标；这些趋势值仍不宣称医疗测量精度。舵机 5 V 在本轮保持断开，未做运动验收。
- 舵机 5 V 仍断开时进行了第一次 LIKE 动作链路采样：`like-follow-servo-off-15s.jsonl`（SHA `5EE5AEAE7B386717B888260CF0518EF4DF43A0F6C450D0417A22CD06B4B8FEDE`）。39/39 个主控显示帧均为 LIKE，CAM 原始分类 30 帧为 LIKE；动作候选框约 134–169 × 158–192，36/39 达到 80×120 近景动作框门槛，证明手势识别/框门槛不是本次阻塞点。
- 该 LIKE 记录未出现 `gesture_action`，模式保持 IDLE、速度全零；同时 40/40 个人物帧均为 `found=false`，状态终点明确为 `wifi_station_disconnected`。动作在采集开始前已由持续手势占用触发锁存窗口，而安全控制器在无 CareRover AP 站点在线时禁止自主运动；即使重新产生边沿，当前人物帧也会返回 `TARGET_NOT_READY`。不降低网络安全门禁。下一次测试要求另一设备保持 `CareRover-EE68` 网页在线，先让手势离场至少 3 个动作帧，脸居中且不被手遮挡，再在主控采集启动后展示 LIKE；PC 不切换网络。
- 网页设备上线后的第一段 `like-follow-online-servo-off-20s.jsonl`（SHA `5F92AAC694B6B5BFEA2A7719C58B91C20D3C25D36BFA9D793863DFA31E49607C`）确认网络门禁恢复：`stop_reason=mode_changed`、`fault/estop=false`；人物检测 13/76 帧成功，置信度 0.593–1.000。但采样内 76/76 手势帧均为 `no_gesture`，没有形成 LIKE 边沿，故保持 IDLE/零速度；这不是动作逻辑失败。
- 为把提示与采集同步，随后启动 45 s 长窗口并在工具运行后提示展示 LIKE；实际记录 `like-follow-online-servo-off-45s.jsonl`（SHA `8076C04C5DEE665BE9F202AC6DBCF334F262200A8675CE60CE6AD913266FED5E`）仍为 179/179 `no_gesture`、人物 0/179 found，说明现场动作与后台即时提示没有同步，不作为 LIKE 功能验收。外部 5 V 供电、未接 CAM USB 的链路本身正常：`vision_link` valid 从 4697 增至 5050，bad 维持累计 1、stale 0、resync 维持累计 1；没有新增链路错误。下一轮改为用户先在网页确认人物框，再采用预先约定的“保持—释放—重新保持”序列，避免依赖实时提示。
- 预定序列的真实重放 `like-follow-online-sequence-servo-off-40s.jsonl`（SHA `6E8F1F42ED6C0C7A21C5ED98874D5042E5ED27F71AA573D42738033523D1B6D4`）成功同步：人物 114/114 found、置信度 0.86–1.00；LIKE 显示 62 帧、其中 46 帧为新鲜非 holding 分类。但没有 `gesture_action`，系统仍为 IDLE/零速度。逐帧检查发现真实肩旁 LIKE 框宽约 80–96、高度多数 114–119；只有 13 个帧偶然同时达到旧 `80×120` 动作框门槛，且没有连续 4 帧，所以四帧动作确认每次都被打断。
- 以现场首四个新鲜框 `83×114、90×120、83×114、81×118` 建立确定性回归。保持旧阈值时 `D:\Anaconda\python.exe tools\check_firmware.py` 仅在 `four observed real LIKE boxes must start follow` 处稳定 FAIL，其他 safety/tracking/drive 测试仍通过。最小修复只把 LIKE 专用高度门槛从 120 降至 110，宽度仍为 80；LIKE 仍必须满足四帧确认、人物新鲜、网页站点在线、IMU/标定/截止期安全门禁。DISLIKE/TWO/OK 继续使用 `80×120`，尤其不放宽可启动原地旋转的 TWO/OK。
- 修复后现场重放和 C++ safety/tracking/drive/tuning 全部 PASS；Python 28/28、Node 14/14 PASS。未增加临时调试日志或绕过安全门禁。新包 `build/stage5-follow-0cf72d947bad0651-device` 校验 PASS：固件 `0cf72d947bad0651-s5-follow`，Web 保持 `079a3a0e1913dd71`，app 1,079,136 B、SHA-256 `6AB9DF8FC4EC477A3CB6D0A4503E0EAB76DBF5775272558077EEE5877CF2D304`，manifest SHA-256 `45346210BC6520F6EE2817B4CBD968A8E094B8B15F4AF62BF6361138AD45FB3C`；分区校验通过。
- COM6 写入前完整备份 `main-before-20260914-231005.bin` 为 16,777,216 B，SHA-256 `E194758AA7BC56C28799D174BB9F10AE6BD9919977CD17FB4F4352B46A2D478D`。守护流程重新确认同一 ESP32-S3/16 MB Flash/8 MB PSRAM/MAC，仅写固件分区且写后哈希通过；NVS 舵机标定和 FFat 网页均未写入。
- `post-like-gate-fix-boot-15s.jsonl`（SHA `E163DA085392F6857C5861C3FBE04222095F01704E9A45FDC4FC915B591FF5E0`）确认新固件运行；15/15 MPU 报告 valid/calibrated、`tilt_fault=false`，系统保持 IDLE、fault/estop false、速度全零，CAM `valid=202/bad=0/stale=0/resync=0`。软件回归已完成，但必须在另一设备重连重启后的 AP 后再次执行真实 LIKE 序列，才能关闭原始现场故障。
- LIKE 修复的原始现场回归 `post-like-gate-fix-online-sequence-30s.jsonl`（SHA `5AA4960A069CF780DE97A4C07AD88FBC1B1BEECF2FE09AD0C1FA090F9FDCEADA`）PASS：出现一次且仅一次 `gesture_action LIKE ok=true`，随后连续 8 个状态窗为 PERSON_FOLLOW。人物 94/97 found、置信度 0.982–1.000；居中时输出为零，水平偏差时只产生小幅 `wz=-0.044～+0.064`，`vy` 始终为零，符合“转向居中、不误横移”。全程 fault/estop false、IMU 无坏状态，舵机 5 V 断开。
- 同一回归在约 9.25 s 因一个短暂人物漏检退出到 IDLE，尽管整段仅 3/97 帧 missing。根因是 combined stream 的 P 帧周期约 312 ms，而 `PersonFollowController::lossGraceMs=300`；首个 missing 帧到达时已略超宽限，错误地被当成永久目标丢失。单帧时 `output_={}` 已能立即停车，问题只在过早退出模式。
- 新增确定性节拍回归：三个有效人物框后在 320 ms 注入一个 missing，要求速度归零但 `lost=false`；下一有效框可继续；第二个连续 missing 使最后有效框超过宽限后必须 `lost=true`。旧逻辑在首个 320 ms missing 处稳定 FAIL。最小修复将检测丢失宽限设为 450 ms，仍小于 Person/CAM 490 ms 硬截止期；没有 P 数据时 490 ms 截止停车不变，单帧 missing 时不输出运动，两帧连续 missing 仍退出 IDLE 且不自动恢复。
- 修复后 C++ safety/tracking/drive/tuning 全部 PASS，Python 28/28、Node 14/14 PASS。最终包 `build/stage5-follow-fffc41be9b0db516-device` 校验 PASS：固件 `fffc41be9b0db516-s5-follow`，Web `079a3a0e1913dd71`，app 1,079,136 B、SHA-256 `ABB44EC9D7D78F68C8589FD18BE6485CF0E121AF64B43B2E37AD786222D96D8A`，manifest SHA-256 `A40B01F3EB7BA6D81FA8BA799FA93A2A240D8C39BAE54AA55180F4FF82218A7D`。
- 最终写入前完整备份 `main-before-20260914-234309.bin` 为 16,777,216 B，SHA-256 `4DAA6FE1F11499B26EF9693F1C021A0E8FD13819FE5F622772EA0C68093B6BC1`。COM6 身份一致，仅写固件分区且写后哈希通过，NVS/FFat 未改。`post-person-miss-fix-boot-15s.jsonl`（SHA `07F2667076AF8CD6F6D6F6892A0D233512EB5A34F13A4084C3B96347C6071A`）确认最终固件、IDLE/零速度、fault/estop false，MPU 15/15 valid/calibrated 且无 tilt，CAM `valid=179/bad=0/stale=0/resync=0`。最终持续跟随现场回归仍待网页重连后执行。

### 2026-09-15 检测、显示与滤波优化交接包

- 按用户要求建立 `CareRover_Demo_Signal_Optimization_Handoff_2026-09-15`，不修改、构建或刷写当前固件。主文档 `01_CURRENT_LINKAGE_AND_THRESHOLDS.md` 完整汇总 CAM 手势/人物、MAX30102、MPU6050、OLED、网页、跟随/动作状态机的进入、保持、退出和现行阈值；`02_DEMO_OPTIMIZATION_IMPLEMENTATION_PLAN.md` 给出按数据类型匹配的证据累积、bbox alpha-beta/Kalman、PPG 软质量评分与一维自适应滤波、IMU 坏样本拒绝/动态互补滤波，以及分阶段实现和验收顺序。
- 交接包复制当前权威源中的 CAM（含受管 ESP-DL 组件）、主控源码（排除 `wifi_secrets.h`）、网页源码、测试、构建/采集工具、现行项目说明和 8 份关键现场 JSONL。未包含 `*.local.json`、真实密码、Flash/NVS 备份、设备 `.bin`、项目 build/toolchain/cache。
- 文档改用中性的分层汇总：检测、显示、动作和运动相关时序分别列出；240 ms输出租约、490 ms CAM/person来源过期、100 ms IMU来源过期作为当前基线和对比测试项记录，不使用“不可突破的安全边界”表述。
- 交接包的 `MANIFEST.sha256` 和外层 ZIP/SHA-256 在打包核验后生成。当前现场保持舵机 5 V断开、四轮架空；最终 450 ms 人物漏检版本尚缺真人连续跟随回归，此边界已在两份文档中明确。
- 交付位置：`ESP32-S3开发/CareRover_Demo_Signal_Optimization_Handoff_2026-09-15` 目录及同名 `.zip`/`.zip.sha256`。外层压缩包经过逐文件 manifest 核验；接手 Agent 从目录内两份主文档开始，按阶段执行，不需要重做已完成的软件集成。
- 另提供不含测试缓存的 clean 交付副本：`ESP32-S3开发/CareRover_Demo_Signal_Optimization_Handoff_2026-09-15_clean` 及同名 `.zip`/`.zip.sha256`；以 clean 副本作为发送给下一位 Agent 的版本，校验值以相邻文件为准。
