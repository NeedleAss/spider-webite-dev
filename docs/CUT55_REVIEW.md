# 已批准并导出：55 秒最终影片

2026-09-19 用户已确认本版画面与配乐并要求输出 MP4。成片已保存至 `/Users/eddysiangz/Downloads/3D建模/CareRover_Final_55s_1080p.mp4`：1920×1080、30 fps、55 秒、H.264 + AAC，约 5.94 MB。完整音视频解码通过；音轨与批准的截取、音量、淡化参考相关系数 0.999275。导出来源见 `evidence/v7/final55-export.json`，验证见 `evidence/v7/final55-validation.json`。仓库原 112 秒媒体仍保留，不冒充本次成片。

以下为导出前的审核记录，其待审核状态已被以上用户批准覆盖。

# 55 秒影片审核候选

预览：`http://127.0.0.1:8765/presentation/?review=55&revision=cut55-2`。载入后暂停，点击底部播放；可拖动进度或逐章查看。

用户已接受上一版画面，本轮修改跟随、横移绕障、节奏与本地配乐。尚未导出新 MP4；须先取得用户对本候选的视觉、节奏审核。仓库 `assets/film-v7/CareRover-film.mp4` 仍为 448d49a 对应的历史 112 秒影片，其 SHA256 为 `e9b02cff21c9d5c17625a4b6cb150e72101ce019d921cc7e0da52bb488b05bb9`，不能当作本轮交付。旧 FILM_MANIFEST 保留其原始来源，当前源码与旧影片来源哈希不同是预期状态，待最终导出后重建并验证。

## 内容与节奏

| 时间 | 内容 |
|---|---|
| 0–1 秒 | 整机正面 |
| 1–7 秒 | 完整拆解与收拢；1.9 秒开始展开 |
| 7–18 秒 | 整机透视摄像头、扫描人头、收回整机、原地转向跟随 |
| 18–26 秒 | 超声特写、回波、恢复整机、保持朝向横移后从杯子侧方通过 |
| 26–30 秒 | 前进、后退、360° 与停稳；省去此处重复横移 |
| 30–38 秒 | TWO、DISLIKE、中立、LIKE 与确认反馈 |
| 38–47 秒 | 接触、采集、OLED 结果、松手 |
| 47–52 秒 | 原有操作台完整 10 秒录制以 2 倍速度播放 |
| 52–55 秒 | 整机结尾 |

`v7/cut55.js` 分章映射原来的 112 秒时间轴。按本次授权，把横移放入绕障过程，省去独立运动段的重复横移（原时间轴 54–60 秒），其余动作保留。默认网页仍保留长版时序供浏览，审核链接使用短版；自由探索仍按已批准的 3.6 秒单程拆合。人脸跟随在两个入口均改为固定机身原点的 yaw 旋转。人头世界位置和朝向独立于摄像头挂载层，稳态摄像头轴线对准目标。展示距离 .1526 m 来自当前保存场景中的人头位置，不是实物标定。

## 配乐

用户提供的 M4A 已自动接入本地预览，时长 234.93 秒、AAC 立体声。候选起点 7.372 秒来自前段频谱起音分析，播放其后 55 秒，开头 0.5 秒淡入、结尾 1.5 秒淡出；仍待用户听感确认，不将信号分析冒充主观试听或音乐审核。源文件哈希、候选起点与依据记录于 `evidence/v7/music-cue.json`。

本地音频位于 `presentation/assets/music-local/review.m4a`，目录被 Git 忽略。本轮未发布音乐或输出新 MP4；审核页右上角可修改起点或换入音频。收到本次画面、节奏与配乐的审核后再导出。

超声场景中的杯子保持世界位置和朝向，摆放底部接近机器人地面；机身先横移 0.15 m，再前进 0.31 m，始终保持 yaw=0。光束按传感器当前位置与杯子网格重新求交，离开杯子后不再显示命中回波。全景取景保持固定，后续章节继承绕行终点并移动相机参照，避免跨章把机身瞬移回原点。此为编排原理动画，不表示固件已具备自主路径规划。

## 本轮验证

- Node：64/64，`evidence/v7/avoid55-node.txt`。
- 浏览器短版：42 项通过，包括固定旋转中心、人头世界朝向、稳态光轴对准、九章保留、反向 seek、操作台完整时间映射、音乐同步及 55 秒终止，`evidence/v7/avoid55-browser.txt`。
- 原展示与自由探索：55 项通过，`evidence/v7/avoid55-regression.txt`。
- 关键画面：`output/playwright/cut55-*.png`；真实连续播放记录：`evidence/v7/avoid55-music-playback.txt`。
- 固件、控制台和配置的 75 个受保护文件哈希未变；无烧录、实体运动或新硬件验收。

最终视觉与音乐验收：等待用户。最终 55 秒 MP4：未执行，按用户要求保留审批顺序。

配乐兼容修复：原始 M4A 经普通本地 HTTP 服务加载时无法可靠 seek，现先读入本地 Blob URL 后交给音频元素，整段重新检查原文件的播放时间与片头偏移。
