# 下一轮 Codex Goal｜把 CareRover 做成真实、克制的深色产品展示

## 目标

在已有 `NeedleAss/spider-webite-dev` 上继续完成产品展示，不重写机器人、不重做机械设计。此次确认的审查基线为 `codex/final-polish@72ee475bd76b7a46f2c3bd6477dcd2f45b76ac69`，PR #1。开始先检查当前分支、工作区和最新远端；保留比该版本更新的组员成果，不强制 reset 或覆盖。

先读本包 `CareRover_Production_Brief_v4.md`、`config/ASSET_CONTRACT.json`、`config/STORYBOARD.json`，再读仓库 `presentation/README.md` 和 `docs/FINAL_ACCEPTANCE.md`。第3版安全问题与实物门槛继续有效。本包不是烧录包，也不是已完成的写实资产。

**第一交付：完整传感器正面 → 滚动开壳 → 摄像头近景与正确方向的目标检测情节。** 这一段真的可看、可反向滚、可在手机翻页后，再做其余章节。不要先交七屏占位卡片。

## 用户本轮已经确认，不再反复追问

白色打印外壳；OLED在顶盖；正面两个圆孔为HC-SR04通道，下方小方孔为镜头；底部为电源；IMU和手指接触健康模块在侧边。4个24158迷你全向轮，24mm/SG90兼容20T；4个MG90S 360°舵机；18650电源模块；ESP32-S3-N16R8主板；ESP32视觉传图板；HC-SR04；MPU6050/GY521；MAX30102；蓝色1306 OLED。

照片为未完成装配，实际量测尺寸、全部线端、超声安装深度尚未确认。不要把这些未知当作停止全部软件工作的理由，但也不能填成虚构测量值。三张重复内部照片只有一个视角。

## 阶段 A：确认资产与安装基准

1. 保存原CAD及旧GLB；原GLB SHA256为 `2310e2bd46a6940497349d1acf00ef70bd8279881a637424b2764c136229d61b`。新建 `presentation/assets/appearance/` 放衍生几何与贴图，并记录来源。
2. 从现有rest安装矩阵开始，使用轴心、板面/安装孔、镜头轴和超声端面作为对齐基准，不将所有替换物强行按包围盒各轴拉伸。
3. 目标装配按用户4舵机声明恢复第四舵机。`config/hidden-servo-restoration.json` 提供原始reference 7矩阵；沿用保存矩阵，不猜镜像位置，留待现场最终核对。原CAD的hidden记录不删除。
4. 正面定义固定为摄像头/双圆孔面。在当前保存GLB中为世界−X，up=+Y。新几何转换后重新验证，不把代码固定值当成实物标定。
5. 顶盖OLED应随顶盖一起开合；实际贴图采用蓝色单色像素和黑色玻璃，保持照片中的四角紧固件。别再改成侧面屏幕。

## 阶段 B：真实外观与线束

先做四种一眼能识别的细节：24mm八滚子轮与白联轴器；深色舵机、金属轴、安装耳和标签；相机板/镜筒/橙排线；HC-SR04蓝板和银色端面。随后补主板双USB、屏蔽罩、排针，以及白色健康板/蓝色IMU板/电源组件。不要用不同载板的网上模型冒充同款。

主板下两条长块已由用户说明是杜邦线代理。先语义分离并检查，不按“低处”“最长”盲删所有三角形。用曲线线束和接头替换；端点未知时保留模块级关联，不猜GPIO。线色保持实物特征，UI颜色精简不意味着把全部线改黑或改青。

拆解线束采用有限松弛、显式断开或非重点淡出。不要无限拉长，不穿板，不在端点没有变化时每帧重建全套高面数管线。`CABLE_CONTRACT.json`不是电路图。

本包参考图片仅供建模比对；商家图片未获得公开贴图授权。对外输出用自行制作纹理/几何和团队自有照片，保留许可及来源记录。

## 阶段 C：摄影机和方向先正确

`instanceRest → appearance → sensorAnchor → effect`。效果继承实例安装旋转/比例；起点在镜头/换能器端面，而非整个部件的bbox中心。

v3锚点文件仅是旧GLB候选，精细外观完成后要重新定位并绑定新assetHash。局部坐标用米；旧root=1000只允许封装在单一适配层。不要同时应用父级继承和重复worldMatrix，造成双重变换。

相机效果沿模块前方：目标出现→图像框→主控判断→短运动/停止。超声沿模块前方：出波→遇障→回波→判断。FOV、波束角、TX/RX没证据就不写成标定值。所有示意明确标注，不能变成激光、全向测距或实时数据。

## 阶段 D：深色七章叙事

1认识整机 → 2打开机身 → 3看见目标 → 4前方测距 → 5整车运动 → 6手指接触与顶部显示 → 7收拢及实物对照。

默认Story，原生滚动唯一驱动章节进度，摄影机由导演镜头控制。Inspect才启用OrbitControls；退出回到原章。默认不再悬停全拆，滚轮不再被同时当作缩放。画布中心/静态fallback允许垂直翻页。

深石墨底、真实白壳、冷蓝强调。使用 `starter/dark-design-tokens.css` 为初值，不覆盖控制台。优先一屏一个主角，明确字号/留白/比例；不要十五个小卡片在首屏竞争。系统字体栈，不分发苹果字体或复制苹果素材。

保存immutable rest TRS；progress确定最终状态，不累加变换。相机/位置用可逆关键帧；部件内部小动画单独时钟，离开章暂停，允许显式重播。reduced-motion仍可读完整内容。移动端构图重排，不简单把桌面缩小。

整车动画遵守现有正交轮系：FL/FR/RL/RR目标为 `[vx+wz,vy+wz,vy-wz,vx-wz]` 再归一化。前进不是四轮永远同向；被动滚子和壳体/轴需分离。示例数学代码不能直接替代实际轮轴与旋转正负标定。

多机/户外仅为可选概念编排，不能宣称群控、协作导航或户外认证。先交单机，之后有余量再加。不能为美术调整新增真实运动功能。

## 测试：不用漂亮文档替代可看的画面

执行本包示例：

```sh
node --test starter/story-math.test.mjs
```

这14项只测新示例的数学约定，不能替代仓库测试。仓库继续执行当前既有Node/Python/C++检查及受影响的构建；本轮主要不应更改固件。

每个里程碑交：1366×768、1440×900、390×844、手机横屏的首帧/中间/末帧；一段正向与反向滚屏；真实设备上画布中心翻页、Inspect退出和减动态效果证据。标明实际浏览器/机器、FPS/网络请求、版本与NOT RUN。

必须新增：
- 首屏和重置同一正面；端面锚点在整车/拆解/近景都朝外。
- 顶盖/壳体无穿模；4轮4舵机；不再出现杜邦线长块；代理删除范围可核对。
- 相同progress多次/反向/刷新/resize一致，100次收拢无漂移。
- 无WebGL/资源失败有真实海报和DOM说明；恢复保留章节。
- 静态与Story时触摸能翻页；只在Inspect接管手势；多指/取消不误选。
- 模拟页面不建立机器人连接，不发送任何动作。
- 真实健康结果/电量/身份识别/群控/超声安装状态不被动画伪造。

## 完成定义与最后取舍

第一段未达到外观与交互要求，不扩大后续章节。写实细节耗时过长时优先关键外观，其他留有标注的简化；不拿通用机器人替代。必要镜头用自制离线预渲染，未做的写NOT RUN。

交付更新后的源码、外观资产及hash、模型/来源说明、screenshots/视频、测试日志、尚未确认的实物项与成套回退方法。原始照片中的个人信息不得进入公开仓库。

**不改watchdog、IMU轴、倾倒阈值、NVS、控制权和动作取消来迁就动画。** 安全任务停摆后的PWM独立撤销仍按原现场门槛处理，视觉验收不能替代实物放行。没有当前明确授权与现场前置条件，不烧录、不让实体轮子运动、不自动合并PR。
