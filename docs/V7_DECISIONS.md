# V7 已批准方向与本轮实施

起点：`codex/v7-samples@1d073b5`（其父提交为 V6 候选 `f6d7969`）。用户已明确接受结构/扫描/回波三段样片，并要求自由探索使用相同的分离与合并方式，继续完善其余章节。下载的 V7 报告作为制作输入；历史 V5/V6 中完整成人、桌面与房间要求被 V7 覆盖。

## 已实现的共同路径

生产故事与自由探索均调用 `v7/assembly.js`。删除 `scene/inspection.js` 原有按类型平面分列布局；不从数组次序配对轮/舵机。顶盖/OLED、主控/支架和镜头/安装架保持组内关系。四轮与四舵机使用固定实例 ID。

`story/timeline.js` 的旧 `partOffset()` 与 `robot.apply()` 留作历史实现，不再用于默认生产页面的姿态计算。V4/V6 旧导演、成人和室内资产仍在 Git 中，但默认入口、静态降级与影片链接均使用 V7，网络检查不加载旧演员/室内/旧长片。

自由探索完整进入、停留展开、选中固定、拖动打断镜头、触屏按钮、完整旋转和缩放；关闭说明返回展开总览。选中时保留安装组或底盘参照，主动“单独看”才仅显示选中实例。退出恢复此前时间并暂停；主动滚动后才继续跟随滚动。

## 新故事

112 秒：正面 → 同路径开壳/收拢 → 表面扫描 → 超声命中与回波 → 前后左右/360°/停稳 → TWO/DISLIKE/中立/LIKE → 跟随/目标失效等待 → 接触/采集/OLED/离开 → 生产操作台 → 正面结束。

手势使用清晰的图像平面符号，保留当前真实指令映射与直接手势语义。沒有重新引入全身人物。手部仅用于接触镜头，采用 CC0 MakeHuman 的完整右手/前臂拓扑与权重；在 Blender 4.5.7 中摆正各指、烘焙动作、重新导入 GLB，并提交中性光三视图与独立接触短片。浏览器只播放烘焙动作，没有通用手指角度或手腕翻转求解。

手部仍是新审核候选：制作自检及锚点测试不代替用户视觉接受；当前成片也为审核候选，可在用户接受后正式冻结。原始三个样片的批准不被冒充为新手模的批准。

## 实施边界

- 固件、根目录控制台的 JS/CSS/HTML、设备配置保持 V6 版本。重跑主机与控制台 fixture 回归，不据此声称已经烧录。
- 共享资产校验工具扩展为 V7，纳入新导演、手部与新影片，保留历史资产的完整性记录。原始 CAD GLB 哈希保持不变。
- 拆合是用户接受的展示路径。真实紧固顺序、退出轴、原生 CAD 全网格运动干涉和最终安装方式仍待核验。
- G01 独立 PWM 撤销、真机 OLED/运动/视频、Windows 和真实手机验收仍未执行。
