# CareRover V7 复审请求

请读取 PR #1 最新 `codex/final-polish`，报告实际读到的完整 HEAD，不要只看 main，也不要把本文件的描述当作亲自复测。

用户已接受 `1d073b5` 的结构/扫描/回波样片，要求其余内容延续同样质感，并明确自由探索沿用同一拆合方式。V7 当前实现依据 `V7_DECISIONS.md`；验收证据在 `V7_ACCEPTANCE.md` 与 `evidence/v7/`。历史 V4/V5/V6 中成人、房间和平面分列展开要求已由最新 V7 覆盖。源指导文件在 `docs/v7/source-briefs/`。

请重点审查：

1. `presentation/v7/assembly.js` 与 `scene/inspection.js`：剧情/自由探索是否共用实际路径；组内关系、四轮/舵机对应、正反向复位、聚焦/主动隔离语义。不要用 AABB 自动宣称机械无干涉。
2. `v7/effects.js`：扫描是否落在表面、传感器变换/单位是否正确；超声网格命中、返回时序、裁切平面是否随装配更新。可视化不等于真实激光/声束。
3. `v7/director.js`、`app.js`：Scroll/Deck/Film共用时间、连续360°、手势当前映射、跟随停止、健康/OLED同一事件、离开清空、上下文恢复。默认路径不得加载旧成人/室内/旧影片。
4. 新手模：`presentation/hand.html`、`assets/hand/` 的中性光三视图、5秒接触影片、Blender源、米制GLB、来源/许可；确认完整五指、指腹接触、手腕连续、无突然翻转。网页只播放烘焙动作。锚点测试不能替代实际画面/网格审查。
5. 亲自播放 `assets/film-v7/CareRover-film.mp4`（112秒）和交互页面后再评价视觉。若无法获取媒体二进制或运行WebGL，请明确说明；仅看到路径/哈希不是已观看。
6. 主控/CAM、根控制台与配置相对 V6 保持不变，检查保护哈希与回归证据；直接手势、手动优先、被动页面不抢权、视频时效和急停不允许回退。

请按“实际复测、源码确认、制作建议、现场待验”区分结论。新手部/成片还在用户视觉审核阶段，G01和所有实物项目仍待现场验收，不得将55项浏览器PASS或影片解码PASS写成实体通过。

最新补充（2026-09-19）：当前为九章112秒，第三章18–38秒合并扫描人头与恢复整机后的左右跟随，不再有独立follow章节。重点核验同一人头的保持、独立世界坐标、目标先动/机身响应、持续扫描和停止回位；对应证据为 `evidence/v7/follow-*`。此前章节时间属于历史。
